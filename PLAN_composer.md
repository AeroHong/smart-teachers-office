# 글쓰기 재설계 — 채널 안에서 바로 쓴다

> 상태: **§7의 1~4번 완료(2026-08-25).** 남은 것은 5번(입력창 교체, 별도 과제)뿐.
> 관련: [PLAN_channels.md](./PLAN_channels.md) · [PLAN_blockEditor.md](./PLAN_blockEditor.md)

---

## 1. 지금 무엇이 문제인가

### 글쓰기가 채널 밖으로 나간다

`/requests/new`는 `WorkspaceLayout`을 **사이드바 없이** 쓴다. 즉 채널을 보다가 '글 쓰기'를
누르면 **2단이 사라지고 3단이 전체 폭을 차지하는 별도 페이지**로 튄다. 그 안에서 다시
왼쪽(글)·오른쪽(설정)으로 갈라진다.

```
채널 보는 중                       글 쓰기 (지금)
┌──┬────────┬──────────┐          ┌──┬─────────────────────┐
│1 │ 2단     │ 3단       │    →     │1 │ 글 본문   │ 설정      │
│레│ 채널목록 │ 대화/캔버스│          │레│           │ (채널·대상)│
└──┴────────┴──────────┘          └──┴─────────────────────┘
                                        ↑ 2단이 사라진다
```

**쓰는 동안 채널이 안 보인다.** 어느 채널에 쓰는 중인지, 그 채널에 무슨 말이 오갔는지가
화면에서 없어진다. 되묻는 말에 답하려고 글을 쓰기 시작하면 정작 그 말이 사라진다.

### 채널 안에서 쓰는데 대상을 또 고른다

채널을 고르면 `channel.memberRule`이 대상 조건으로 복사된다(`PostNew.jsx` `pickChannel`).
그런데 **`TargetPicker`가 그대로 펼쳐진 채로 있어서**, 쓰는 사람은 여전히 "대상을 고르는
화면"을 마주한다. 이미 정해진 것을 다시 정하라고 묻는 셈이다.

채널은 **"이 글이 어디에 사는가"**, 대상은 **"누가 해야 하는가"** 로 서로 다른 축이다.
하지만 채널 안에서 쓰는 글은 **기본적으로 그 채널 사람들이 대상**이고, 좁히는 것은 예외다.
지금은 예외가 기본값 자리를 차지하고 있다.

### '채널 없음' 선택지가 아직 남아 있다

P3-A에서 전체 공지 채널로 채널 없는 글을 전부 옮기고 **"모든 글이 채널을 갖는다"** 를
규칙으로 삼았다. 그런데 글쓰기 화면의 채널 고르기에는 `채널 없음` 항목이 그대로 있다.
**데이터 규칙과 UI가 어긋난 상태다.** 지금 누르면 다시 채널 없는 글이 생긴다.

### 입력창이 지금 구조로는 더 못 간다

`RichTextEditor.jsx`는 `contentEditable` + `document.execCommand`다. 우회가 쌓이고 있고
블록 단위 조작이 불가능하다 — 자세한 것은 [PLAN_blockEditor.md](./PLAN_blockEditor.md)에
이미 정리돼 있다. **입력창 전면 교체는 그 문서의 갈래 A(TipTap 교체)와 같은 결정이다.**

---

## 2. 확정 방향 — 3단 안에서 쓴다

**1·2·3단 구조를 그대로 유지한 채**, 3단 안에서 글을 만든다. 별도 페이지로 튀지 않는다.

```
┌──┬─────────┬───────────────────────┐
│1 │ 2단      │ 3단                    │
│레│ 채널목록  │  # 채널명   [메시지][캔버스…][＋] │
│일│ DM       │  ─────────────────────│
│  │ 디렉터리  │  메시지 스트림           │
│  │          │  ─────────────────────│
│  │          │  [입력창]         ＋    │
└──┴─────────┴───────────────────────┘
                                  └ 여기서 캔버스를 바로 만든다
```

- 채널 머리 탭 줄의 **`＋`** 로 새 캔버스를 만든다 (Slack의 `＋` 자리와 같다)
- 만들면 **탭이 하나 늘고 그 자리에서 바로 쓴다.** 저장하면 그 탭이 캔버스가 된다
- 입력창 옆 **`＋`** 는 지금처럼 **기존 캔버스 붙이기**(이미 구현됨)

**채널이 계속 보인다는 것이 요점이다.** 옆 탭에 오간 말이 그대로 있고, 쓰다 말고 돌아갈 수
있고, 주소도 채널 아래에 남는다.

### 확장 — 다른 유형의 문서

`＋`가 지금은 "업무 글 하나"만 만들지만, 나중에 무엇을 만들지 고르는 자리가 된다.

```
＋ → 업무 글 (마감·대상·완료)
    회의록          ← 예정
    체크리스트       ← 예정
    표/명단          ← 예정
```

그래서 **`＋`를 "글쓰기 버튼"이 아니라 "문서 종류 고르기"로 설계해 둔다.** 지금은 항목이
하나뿐이라 눌러도 바로 열리지만, 자리는 처음부터 그렇게 잡는다.

---

## 3. 용어 정리 — 지금 세 벌이 섞여 있다

같은 것을 코드·화면·논의에서 각각 다르게 부르고 있다. **이것부터 하나로 맞춘다.**

| 대상 | 코드 | 지금 화면 | 논의에서 | **확정** |
|---|---|---|---|---|
| 업무 문서 | `requests` | 업무 글 | 캔버스 | **캔버스** (화면), `requests`(코드는 유지) |
| 채널 안 대화 | `messages` | **대화** | 메시지 | **메시지** |
| 1:1 대화 | `type: 'dm'` | 다이렉트 메시지 | DM | **다이렉트 메시지** |
| 옛 1:1 | `personalNotices` | 쪽지 | 쪽지 | **쪽지** (P7에서 동결) |
| 가리켜 보내기 | `refRequestId` | 전달 | 넘기기/공유 | **전달** |

**바꿀 것**
- 채널 머리 탭 **`대화` → `메시지`**. 쿨메신저를 쓰던 사람에게 '대화'는 1:1 창을 가리키는
  말이라 다이렉트 메시지와 뜻이 겹친다
- **`업무 글` → `캔버스`**? — **아직 정하지 않았다.** '캔버스'는 Slack에서 온 말이라 교사에게
  낯설고, '업무 글'은 뜻이 바로 통한다. **화면 용어는 `업무 글`로 두고 논의에서만 캔버스라고
  부르는 쪽**이 나을 수 있다. 이 문서에서는 편의상 캔버스로 적었다
- 코드의 `requests`는 그대로 둔다. 컬렉션 이름을 바꾸면 마이그레이션이 필요한데 얻는 것이
  이름뿐이다

---

## 4. 대상 선택 — 기본은 채널, 좁히기는 접어둔다

```
지금                              바꿀 것
┌────────────────┐               ┌────────────────────────┐
│ 채널: [전체 공지 ▾]│               │ 이 채널 참여자 61명이 대상  │
│ ─────────────  │      →        │              [대상 좁히기 ▾]│
│ [대상 조건 선택기]  │               └────────────────────────┘
│  (항상 펼쳐짐)    │                    ↑ 눌렀을 때만 TargetPicker
└────────────────┘
```

- 채널 안에서 쓰면 **대상은 이미 정해진 것으로 보고 한 줄로 알린다**
- 좁힐 일이 있으면 그때 편다. 실제로 흔치 않은 경우다
- **`채널 없음` 선택지를 없앤다.** "모든 글이 채널을 갖는다"가 규칙이 됐다
- 채널을 옮길 일은 남긴다 — 다만 그건 쓰는 중이 아니라 **쓴 뒤에 '전달'로** 하는 일이다

---

## 5. 입력창 전면 교체

[PLAN_blockEditor.md](./PLAN_blockEditor.md)의 판단이 여기서 다시 필요해진다. 그 문서는
"업무 글 편집기를 블록 편집기로 바꿀 것인가"를 물었고, 이제는 **채널 안에서 쓰게 되면서
입력창이 두 종류**가 된다.

| 자리 | 성격 | 필요한 것 |
|---|---|---|
| **메시지 입력창** | 짧은 말, 평문 | 지금 `TextField` + 캔버스 붙이기로 충분 |
| **캔버스 편집기** | 긴 문서, 서식 | 블록 편집기 — `PLAN_blockEditor.md` 갈래 A |

**둘을 같은 물건으로 만들지 않는다.** 메시지에 서식을 허용하면 정화기(`sanitizeHtml`)가 한 벌
더 늘고 한 군데만 빠뜨려도 XSS가 된다 — `channelMessages.js`에 이미 적어둔 판단이다.

착수 전에 `PLAN_blockEditor.md`의 "먼저 확인할 것" 세 가지를 먼저 재야 한다(TipTap 번들
증가량 실측 포함).

---

## 6. 전반적인 디자인 정리 (범위만 적어둠)

사용자 지적: "다른 부분은 전반적인 수정이 필요하고 (디자인 적으로)".

구체적인 항목은 아직 안 뽑았다. **다음 작업 때 화면을 함께 보면서 정한다.** 지금까지 나온 것:

- 사이드바에 `#` 기호가 없다 — 채널 머리에만 있다. 넣을지 결정 필요
- 채널 머리·탭 줄·입력창의 여백과 위계가 화면마다 조금씩 다르다
- 빈 상태 문구가 자리마다 따로 논다

---

## 7. 착수 순서

1. [x] **용어 통일** (§3) (2026-08-25) — 채널 머리 탭 `대화` → `메시지`.
       DM 쪽(`DmDialog`·`Directory`의 "대화" 버튼)은 그대로 뒀다 — 거기서는 "대화 상대를
       고른다"는 동작 이름이라 뜻이 안 겹친다. 겹치던 자리는 채널 탭 하나뿐이었다
2. [x] **대상 선택 정리 — `채널 없음` 제거** (2026-08-25) — `PostNew.jsx`. 채널을 안 고르고
       들어오면 **전체 공지 채널**(`ALL_STAFF_CHANNEL_ID`)을 기본값으로 둔다. 그 채널의
       `memberRule`이 빈 조건(전체 교직원)이라 예전 "채널 없음"과 대상 결과가 완전히
       같다 — 동작은 그대로고 데이터만 규칙(모든 글이 채널을 갖는다)에 맞게 채워진다.
3. [x] **3단 안으로 이동** (§2) (2026-08-25) — 아래 "§7-3·4 완료" 절 참고.
4. [x] **`＋` 를 문서 종류 고르기로** (§2) (2026-08-25) — 아래 절 참고.
5. [ ] **입력창 교체** (§5) — 별도 과제. `PLAN_blockEditor.md` 판단이 먼저

---

## 7-3·4 완료 (2026-08-25) — `PostComposer.jsx`

`PostNew.jsx`(별도 페이지, 1240px 2단 레이아웃)를 지우고 `components/PostComposer.jsx`로
옮겼다. `Channels.jsx`가 자기 3단 자리에 직접 얹는다 — 페이지 전환이 아니라 같은 화면
안에서 탭 하나가 늘어나는 것으로 보인다.

**주소**: `/channels/:channelId/new`(새 글) · `/channels/:channelId/:requestId/edit`(고치기).
§8의 첫 번째 질문에 대한 답 — **문서를 먼저 만들지 않는다.** 저장하기 전까지는 진짜
`requests` 문서가 없고, 탭 줄에는 `새 글`이라는 임시 탭만 떠 있다가 저장하면 그 자리를
진짜 캔버스 탭이 대신한다. 취소하거나 다른 탭으로 옮기면 임시 탭은 그냥 사라진다 —
**초안을 남기지 않기로 했다**(§8의 두 번째 질문). Slack의 "초안 및 전송됨"에 대응하는
것을 만들지 않은 이유는, 지금 규모에서 그 인프라(초안 목록·자동 저장·정리)를 갖출 만큼
쓰다 마는 일이 잦다고 볼 근거가 없어서다 — 필요해지면 그때 데이터로 판단한다.

**채널은 프롭으로 고정, `<Select>`가 없다.** §4에서 정한 대로 "채널을 옮기는 일은 쓴
뒤에 전달로"가 된 것은 새 글만이 아니라 **고치기도 마찬가지다** — §8의 네 번째 질문에
대한 답이기도 하다. 고치기도 같은 `PostComposer`를 쓰고 채널은 그대로 고정이다. 채널을
프롭으로 고정하니 "채널이 아직 안 정해졌을 때"라는 상태 자체가 없어져 `채널 없음`
제거(2번)의 나머지 절반 — 좁히기 접기 — 도 자연히 여기서 마무리됐다: 대상은 기본으로
접혀 있고 "이 채널 참여자 N명이 대상입니다"라는 한 줄만 보이다가, 누르면 `TargetPicker`가
펼쳐진다. 다만 **고치는 글이 채널 전원과 이미 다르면 처음부터 펼쳐서 보여준다** — 접어
두면 좁혀 놓은 대상을 고치는 사람이 놓치고 "채널 전체"로 착각한 채 저장할 위험이 있다.

**`＋`는 지금 메뉴가 아니라 바로 이동하는 버튼이다.** §2가 그린 "문서 종류 고르기"는
항목이 하나뿐인 지금 열어봐야 고를 게 없는 메뉴라, 아직 쓰지도 않는 메뉴 인프라를
미리 만들지 않았다(YAGNI) — 채널 탭 줄의 아이콘 버튼이라는 **자리**만 그 역할을 하도록
잡아 뒀다. 회의록·체크리스트 같은 두 번째 종류가 실제로 생기면, 이 버튼의 클릭 핸들러를
목록 길이로 분기해 메뉴로 바꾼다.

**나머지 진입점도 전부 옮겼다** — `RequestList.jsx`(요청 현황)와 전역 `CommandPalette.jsx`
(⌘K)의 "글 쓰기"는 채널 맥락이 없는 자리라, 기본값인 전체 공지 채널(`/channels/all-staff/new`)로
보낸다. `PostDetail.jsx`의 "수정" 버튼은 글에 이미 있는 `channelId`로 바로 간다.

**옛 주소는 리다이렉트를 안 만들었다.** `/posts/:id`(보기 링크)는 쿨메신저에 이미 뿌려진
것들이 있어 `PostRedirect.jsx`로 살려뒀지만, `/requests/new`·`/requests/:id/edit`은 앱
안에서 버튼을 눌러야만 가던 주소라 밖에 뿌려진 링크가 없다 — 죽여도 되는 주소였다.

---

## 8. 착수 전 판단이 필요한 것 — 전부 결론 남

- [x] **화면 용어를 `업무 글`로 둘지 `캔버스`로 바꿀지** (§3) → **`업무 글`로 둔다.**
      교사에게 낯선 'Slack 캔버스' 대신 뜻이 바로 통하는 말을 썼다. 이 문서·설계 논의에서만
      편의상 캔버스라 부른다.
- [x] 3단 안에서 쓸 때 **주소를 어떻게 둘지** → **`/channels/:channelId/new`.** 문서를
      먼저 만들지 않는다 — 위 "§7-3·4 완료" 절 참고.
- [x] 쓰다 만 글(초안)을 남길 것인가 → **안 남긴다.** 위 절 참고.
- [x] 고치기(`/requests/:requestId/edit`)도 3단 안으로 넣을지 → **넣었다**
      (`/channels/:channelId/:requestId/edit`), 새 글과 같은 `PostComposer`를 쓴다.
