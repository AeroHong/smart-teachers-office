# 시간표/선택과목 편성 시스템 — 아이디어 정리

> 작성일: 2026-07-29
> 상태: 미착수 (아이디어 단계, 구체적 설계 전)
> 우선순위: 낮음 (다른 교사가 만든 시간표 프로그램을 보고 도전 의욕이 생겨 정리해둔 메모)

---

## 배경

향후 다음 기능들을 만들어보고 싶다는 아이디어:

- 교사 시간표 작성
- 학생 선택과목 분반 편성 (2·3학년은 학생마다 선택과목 조합이 달라 반이 과목별로 갈림)
- 선택과목별 명렬표 작성
- 개인별 시간표 작성

이 기능들은 교사×과목×분반×시간대, 학생×선택과목 같은 다대다 관계가 핵심이라, 지금까지의 스마트교무실 기능(업무 대시보드, 출결, 보강신청 등)보다 훨씬 "관계형스러운" 데이터 추론이 필요함.

## DB 구조 논의 요약 (2026-07-29 대화)

**결론: 기존 시스템은 Firestore를 그대로 유지.** 전면적으로 관계형 DB(Supabase 등)로 옮기지 않음.

이유:
- 이미 라이브 운영 중인 시스템이 Firestore 보안 규칙(`firestore.rules`)에 깊이 의존 — 클라이언트가 서버 없이 직접 DB에 접근하는 구조의 핵심이 이 규칙임. 관계형 DB로 옮기면 이 모델 자체가 사라지고 API 서버를 새로 만들어야 함(Auth·Cloud Functions·기존 데이터 마이그레이션까지 포함하면 사실상 전체 리라이트).
- 시간표 편성처럼 복잡한 제약조건(교사 중복 배정 방지, 학생 선택과목 시간 충돌 방지, 분반 정원 등)을 푸는 건 DB 종류의 문제가 아니라 **알고리즘의 문제** — SQL의 JOIN도 이런 제약조건을 자동으로 풀어주지는 않음. 관계형 DB로 바꿔도 결국 애플리케이션 코드가 이 로직을 짜야 함.
- Supabase(Postgres 기반)는 새 프로젝트라면 좋은 선택이지만, 지금 시스템에 도입하는 이득(관계형 모델링 편의성)보다 전환 비용이 훨씬 큼.

**대신 채택할 패턴: "편성 단계"와 "조회 단계"를 분리**

1. **편성/생성 단계** (실제 관계형 추론이 필요한 부분)
   - 분반 편성, 개인별 시간표 생성 등은 Cloud Function 안에서 관련 데이터(학생 선택과목 신청, 교사 가능 시간, 분반 정원 등)를 전부 메모리로 읽어들인 뒤, 일반 JS 배열/객체로 제약조건 계산을 수행하고 결과만 Firestore에 씀.
   - 이 단계는 SQL의 JOIN이 필요 없음 — 어차피 계산하려면 전부 메모리에 올려야 함.
   - 학기초 1회성 배치 작업 성격이라 실시간성도 필요 없음.
   - 완전 자동 배정(제약조건을 알고리즘이 스스로 만족시키는 방식)은 그 자체로 큰 프로젝트이므로, 1차는 관리자가 수동/반자동으로 배정하는 화면부터 시작하고 자동화는 이후 단계로 미루는 것을 권장.

2. **조회/열람 단계** (결과를 보여주는 부분)
   - 명렬표, 개인별 시간표 열람은 이미 계산이 끝난 결과를 보여주기만 하면 되므로 Firestore가 잘하는 영역. 예: `분반/{id}.studentIds`, `students/{id}.timetable` 같은 형태로 결과만 저장해두고 조회.

## 기존 컬렉션 조사 결과 (재사용 가능성 확인용, 2026-07-29 조사)

- `schools/{id}/courses` — 출결 모듈 전용 단순 이름표(`{name, createdBy, createdAt}`). 학년·교사·학생 배정 없음. 재사용 어려움.
- `schools/{id}/asaSubjects` — 성취평가제 체크리스트 전용. `{name, grade, semester, teacherEmails[], achievementLevel}`. 학생 명단·분반 개념 없고 연도(year) 필드도 없음. 참고는 되지만 그대로 재사용하면 ASA 체크리스트 워크플로우와 충돌 위험.
- `schools/{id}/studentGroups` — 출결 QR 세션 전용(`{name, studentIds[], shared, createdBy, ...}`). 구조는 비슷해 보여도 출결 모듈 가정에 강하게 결합돼 있어 재사용 시 충돌 위험.
- 동아리(club) / 분반 / 교육프로그램 출석 — **기존 코드에 전례 없음.** 완전 신규 영역.
- 연도별(학년도) 데이터 컨벤션: 이 코드베이스는 `schoolYears/{year}/...` 같은 서브컬렉션 구조가 아니라 `asaCutoffs`처럼 **평평한 컬렉션 + `year`/`semester` 필드를 두고 클라이언트에서 필터링**하는 패턴을 씀. 새 기능도 이 컨벤션을 따르는 게 일관적.

## 다음에 착수한다면

1. 범위를 훨씬 좁혀서 시작 — 예: "선택과목 신청만" 또는 "분반 명렬표 열람만" 같은 단일 기능부터.
2. 데이터 모델: `schools/{id}/electives/{year}_{subjectId}` 같은 평평한 컬렉션(연도 포함), 분반은 `groups: [{name, studentIds[], teacherUid}]` 형태로 서브필드 또는 별도 컬렉션.
3. 배정 화면은 관리자가 드래그앤드롭 또는 표 형태로 수동 배정하는 것부터 (자동화는 나중).
4. 시간 충돌 검증 로직은 Cloud Function 콜러블로 분리 — 저장 전에 검증 결과를 반환.
