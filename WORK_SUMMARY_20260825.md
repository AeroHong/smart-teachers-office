# 작업 요약 — 2026-08-24 ~ 08-25 (채널 재편 P2b · P3 · 디렉터리)

다른 PC에서 이어서 작업하기 위한 인수인계 문서. 무엇이 됐고, 무엇이 안 됐고, **무엇이
고장 나 있는지**를 적는다.

배포: https://smart-school-dashboard.web.app · 저장소: AeroHong/smart-teachers-office (main)

---

## 1. 이번에 끝낸 것

| 단계 | 내용 | 커밋 |
|---|---|---|
| **P2b** | DM — 이름 없는 2인 채널 | `9283f5b` |
| **P3-1** | 캔버스 = 탭 하나당 문서 하나 (자동 아카이브 + 수동 되돌리기) | `3680fc0` |
| **P3-2** | 빈 채널 첫인상 (`ChannelIntro`) | `3680fc0` |
| **P3-D** | 디렉터리 — 사용자 · 채널 · 사용자 그룹 | `4adcd5e` |
| **P3-A** | 전체 공지 채널 + 캔버스 전달 | `5d3da85` |
| 수정 | 전체 공지 채널이 사이드바에서 사라지던 문제 · 전달/첨부 UI 개편 | `f588294` |

검증: 순수 테스트 **226개**, 규칙 테스트 **54개** 전부 통과.
`npm test` / `npm run test:rules`

### 데이터 마이그레이션 (실제 적용됨)

`functions/migrations/moveOrphanPostsToAllStaff.js` — seonyoo-hs에만 적용.
`schools/seonyoo-hs/channels/all-staff` 생성(이름 **전체 공지**, 참여 61명).
**이제 모든 글이 채널을 갖는다** — "채널 없는 글" 예외가 사라졌다.

```bash
# 미리보기 / 적용
node functions/migrations/moveOrphanPostsToAllStaff.js --school=seonyoo-hs
node functions/migrations/moveOrphanPostsToAllStaff.js --school=seonyoo-hs --apply
```

빈 테스트 학교 5곳은 건드리지 않았다. 필요하면 `--school=`로 따로 돌린다.

---

## 2. ⚠ 알려진 버그 — 다음에 제일 먼저 볼 것

### 채널 참여자 명단에서 본인이 빠진다 (원인 미상)

**증상.** 마이그레이션이 교직원 61명을 명단에 넣었는데, 45초 뒤 클라이언트 쪽 쓰기가
60명으로 덮으면서 **로그인한 본인만 빠졌다**(`createdAt` 15:13:07 → `updatedAt` 15:13:52).
사이드바 목록 쿼리가 `memberUids array-contains me`라, 명단에서 빠지는 순간 그 채널이
목록에서 통째로 사라진다. **화면에는 아무 일도 없어 보인다.**

**부장회의 채널에서도 같은 상태를 봤다** — 만든 사람인데 `memberUids`에 없었다. 직급은
부장(`positionLabel: 교무부장`)이고 2026 배정 문서도 정상이라 조건(`rank in [부장,관리자]`)에
분명히 맞았다. **다만 그 채널은 사용자가 직접 지웠으므로 더 조사할 수 없다** — 두 번째
사례로 세지 말 것. 지금 남은 증거는 전체 공지 채널 하나뿐이다.

**확인한 것 / 아닌 것**
- `resolveTargets`에 본인 제외 로직 없음 — 빈 조건이면 전원이 대상
- `useSchoolMembers`의 `STAFF_ROLES`에 `school_admin` 포함됨
- `firestore.rules`의 `users` list 규칙 정상(같은 학교면 조회 가능)
- `leftUids`는 비어 있음, `archived`도 false

**남은 의심 경로.** `memberUids`를 덮을 수 있는 곳은 둘뿐이다 —
`refreshChannelMembers`(참여자 갱신)와 `saveChannel`(채널 고치기). 둘 다
`resolveTargets(rule, members)` 결과를 그대로 넣는다. 재현하려면 **브라우저에서 갱신을
눌러 보고 그때 `members`가 몇 명인지 찍어봐야 한다.**

**지금 걸어둔 방어(부분적).** `useChannels`가 전체 공지 채널만 문서를 **직접 구독**한다.
이 채널은 "학교 사람이면 전부"가 정의상 참이라 명단을 거칠 이유가 없다.
**일반 채널에는 안 듣는다.**

### 화면이 통째로 안 뜨던 문제 — 해결됨 (2026-08-25)

`ErrorBoundary`가 "화면을 표시하지 못했습니다"를 띄웠다. **권한 문제가 아니라 `Button`
import 누락이었다**(`ChannelMessages.jsx`). 계정 권한은 정상이다 —
`role: school_admin` → `isAdmin: true`(AuthContext 298행).

무서운 것은 **터지는 시점이 미뤄졌다는 점**이다. `Button`을 쓰는 자리가 원래
`canvases.length > 0`으로 가려져 있었고 학교에 업무 글이 0건이라 한 번도 렌더되지 않았다.
"붙이기 단추를 항상 보이게" 고치는 순간 비로소 터졌다. **빌드는 통과한다** — Vite는 정의되지
않은 식별자를 잡지 않고, 이 저장소에는 ESLint 설정이 없다.

- 재발 방지로 할 수 있는 것: ESLint(`no-undef` + `react/jsx-no-undef`) 도입.
  **아직 안 했다** — 별도 과제
- 임시 대비책: 컴포넌트를 새로 쓰거나 JSX를 옮긴 뒤에는 **실제로 그 자리를 렌더시켜 보고**
  배포한다. 빌드 성공만으로는 아무것도 보장되지 않는다

---

## 3. 확인 결과에 대한 답 (2026-08-25 사용자 확인)

| 확인 사항 | 실제 원인 |
|---|---|
| 사이드바에 `#` 기호 없음 | **의도된 현재 동작.** 사이드바는 이름만, 비공개만 자물쇠. `#`는 채널 머리에만 |
| `이 글 전달` 메뉴 없음 | **배포 문제 아님.** 캔버스가 열려 있을 때만 뜨는데 학교 전체에 업무 글이 **0건**이라 열 캔버스가 없었다 |
| `＋ 업무 글 붙이기` 없음 | 같은 원인. `canvases.length > 0`으로 감췄었다 → **고침**: 이제 항상 보이고 비어 있으면 "아직 업무 글이 없습니다"가 뜬다 |

> 배포는 정상이었다. 로컬 빌드와 배포본 해시가 일치함(`index-CVQfnbQW.js`)을 확인했다.
> **있는 기능이 조건에 따라 사라지면 사용자는 조건 때문인지 고장인지 구분할 수 없다** —
> 이번에 감춘 게 잘못이었고, 그래서 빈 상태를 보여주는 쪽으로 바꿨다.

**다음에 확인하려면 업무 글부터 하나 만들어야 한다.** 지금 학교에 글이 0건이다.

---

## 4. 다음 설계 방향 — 메시지 단위 스레드 (사용자 지적, 미구현)

Slack의 실제 구조는 이렇다.

```
채널 → 메시지 하나하나가 스레드의 뿌리
        └ 리액션(이모지)
        └ 스레드 댓글
        └ 공유("이 메시지 전달" → 채널 또는 사용자 검색)
```

**우리와 어긋나는 지점.** 지금 우리 '전달'은 **캔버스(업무 글) 단위**다. Slack은 **메시지
단위**다. 그래서 지금은 대화 중에 오간 말 하나를 다른 채널로 넘길 방법이 없다.

미구현 셋:
- [ ] **메시지 리액션** — 이모지. `completedUids`(완료 체크)와 겹치는 개념이라 통합 여부
      판단이 P4에 걸려 있다
- [ ] **스레드 댓글** — 데이터모델 §6에서 **v1 제외로 확정**했던 것이다. 넣으면 읽음 처리·
      알림·검색이 전부 두 겹이 된다. **이 결정을 다시 볼지가 먼저다**
- [ ] **메시지 단위 전달** — 지금 `ShareCanvasDialog`가 하는 일을 메시지에도 열어주면 된다.
      `refRequestId` 대신 `refMessageId`가 필요하고, 그러면 메시지가 메시지를 가리키게 된다

용어도 맞춰야 한다: 채널 머리의 **`대화` 탭 → `메시지`** 로 이름 변경.

---

## 5. 레일 구조 재편 — 현재 vs 목표

`PLAN_channels.md`의 "레일 구조 재편" 절에 자세히 적혀 있다. 요약만:

**3단 구조는 이미 있다** (`AppRail` 64px → 사이드바 268px → 상세). 바꿀 것은 **1단 메뉴 구성**뿐.

```
현재 1단                        목표 1단
홈 / 요청 현황 / 채널 /          홈(=채널) / 내 활동 / 학사일정 / 관리자
쪽지 / 구성원 / 설치 현황
```

**남은 선행 조건은 하나.** 홈의 `요청받은 일`이 갈 자리 = 내 활동의 `안 한 일`.
그건 P4(멘션)와 함께 만든다.

> 알림은 읽으면 사라지고 **할 일은 하기 전에는 안 사라진다.** 요청받은 일을 걷어내면서
> 그 신호까지 없애면 안 된다.

- [x] 채널 없는 글의 자리 → 전체 공지 채널로 해결됨
- [ ] 쪽지·구성원 1단 항목을 접는 시점 → **P7까지 유지**. 구성원 화면의 "여러 명 골라 쪽지
      보내기"가 지금 쪽지의 주된 창구고, 계획서가 "DM이 쓸 만하다고 확인된 뒤에 쪽지를
      닫는다"고 못 박았다

---

## 6. 다음 작업 후보

1. **참여자 명단 버그 재현·수정** (§2) — 다른 채널도 조용히 사라질 수 있어 우선순위 높음
2. **P4 — 리액션 · 멘션 · mute** — 내 활동의 재료가 되고, 손으로 묶는 사용자 그룹
   (`@2학년담임` 멘션 핸들)도 여기서 함께 설계한다
3. **메시지 단위 스레드/전달** (§4) — 스레드 v1 제외 결정을 다시 볼지가 먼저
4. **P3-3 홈 재구성** — 1번·2번이 끝나야 착수 가능

---

## 7. 다른 PC에서 시작할 때

```bash
git pull
npm install                      # @firebase/rules-unit-testing 등 devDeps
npm test                         # 순수 226개
npm run test:rules               # 규칙 54개 (firebase emulators 필요)
npm run dev:dashboard            # 로컬 실행

# 배포
npm run build:dashboard
npx firebase deploy --only hosting:dashboard
npx firebase deploy --only firestore:rules      # 규칙을 고쳤을 때만
```

마이그레이션 스크립트는 `gcloud auth application-default login`이 한 번 필요하다.

**빌드 산출물(`dashboard/index.html`)은 배포할 때마다 별도 `chore:` 커밋으로 올린다** —
저장소 관례다.
