# 채널 — 업무 중심 재편 논의

> 작성일: 2026-08-24
> 상태: **설계 정교화 단계. 지금 채널을 만들어 배포하는 게 목적이 아니다.** 목적은 "채널을
> 활용한 업무 시스템 구조"를 Slack과 대조해가며 무엇을 채용하고 무엇을 독자설계할지 결정하는
> 것 — 아래 "Slack 대비 채용/독자설계/제외" 표가 이 문서의 핵심이다. 업무 분석 데이터(사용자
> 직접 작성 예정)가 나온 뒤에야 채널 세분화 단위 같은 실행 단계 결정으로 넘어간다. 이 문서는
> 그때까지의 논의를 잃어버리지 않으려고 남긴다 — 이전에 같은 논의를 다른 PC에서 하다 파일로
> 남기지 않아 통째로 유실된 적이 있다.
> 관련 문서: [DB_STRUCTURE.md](./DB_STRUCTURE.md) `#schools/{schoolId}/channels/{channelId}` 절,
> [PLAN_messenger.md](./PLAN_messenger.md), [PLAN_dashboardElectron.md](./PLAN_dashboardElectron.md)

---

## 문제의식

채널 기능(`c4b51e4` 등, 2026-08-02 병합)은 이미 배포돼 있지만, 지금 구조는 **"업무(요청)가 먼저
있고, 조건(부서/교과/직급 등)으로 대상을 뽑고, 채널은 그 요청들을 모아 보여주는 라벨"**이다.
Slack은 반대 방향이다 — **채널(주제 공간)이 먼저 있고, 그 안에서 누가 할지가 정해진다.**

이 차이가 "담당자 중심 구조를 업무 중심으로 바꿔야 하는가, 그렇다면 담당자는 어떻게 연결되는가"
라는 질문의 정체다. 결론은 아직 없고, 아래는 이 질문을 풀어가며 나온 관찰과 원칙들이다.

---

## Slack 대비 채용 / 독자설계 / 제외 (2026-08-24 정리, 같은 날 저녁 확정 반영 — 이 문서의 핵심)

Slack의 모든 기능을 그대로 베끼는 건 의미가 없다는 게 애초 전제였다. 아래는 1차 정리(낮)를
사용자가 항목별로 검토해 확정한(저녁) 최종 버전이다. 취소선 없이 **최종 결정만** 남긴다.

### A. 이미 채용됨 (구현·배포 완료, 단 접근 경로는 손볼 것 하나 있음)

| Slack 개념 | 우리 구현 | 비고 |
|---|---|---|
| 채널(주제 공간) + 멤버 | `channels` + `memberRule` | |
| Canvas(채널에 딸린 문서) | `requests`(요청·안내, 블록 에디터 서식 본문, `channelId`로 연결) | **접근 루트 변경 필요** — 지금은 채널 안 "업무 글 목록"의 항목 하나일 뿐이라 Slack/Notion처럼 채널 헤더의 전용 탭 같은 독립된 진입 경로가 없다. 아래 C-2와 함께 다시 설계 |
| 스레드/댓글 | `channels`의 `comments` | |
| 상태 표시(온라인/자리비움 등) | `presence`, `PresenceWidget`, Phase B-3 자동 재실 감지 | |
| 채널 보관·나가기 | `archived`, `leftUids` | |

### B. 채용 확정 — 전부 구현 필요 (2026-08-24 저녁 확정)

| 기능 | 방향 |
|---|---|
| 즐겨찾기(핀) | 개인화 상태, `users/{uid}`에 저장(`dashboardLayout`과 같은 자리) |
| 섹션(채널을 사용자가 임의로 그룹핑) | 위와 동일 패턴 |
| DM(쪽지) → 채널/업무글 링크 | `personalNotices`에 참조 필드 추가. "쪽지=포인터, 업무글=캔버스" 원칙 |
| 홈 화면 외부 링크(구글드라이브·스마트교무실 등) | 가벼운 정적 목록 |

### C. 독자설계 — 항목별 확정 (2026-08-24 저녁)

1. **멤버십 방식 — 변경 없음, 현행 유지 확정.** 지금도 수동 초대가 가능하고, 학교
   워크스페이스(구글 계정) 기반으로 이미 연동돼 있어 **외부 사용자 초대 기능 자체가 필요
   없다** — Slack의 "게스트 초대"에 대응하는 요구가 우리 맥락엔 없다. `targeting.js` 조건
   기반 자동 판정 + 수동 초대 병행 구조를 그대로 유지한다.
2. **채널 콘텐츠 — 방향 수정: "메시지 + Canvas" 하이브리드로 간다.** 원래 "대화 스트림이
   아니라 업무 글 목록"이라던 독자설계를 뒤집는 결정이다. Slack 구조처럼 채널 상단엔
   메시지(대화) 영역을, 그와 별도로 Canvas 기능을 둔다.

   **열린 질문 해소 (2026-08-24 저녁)**: 8/2에 이 결정을 피했던 이유는 "쿨메신저가 이미
   실시간 대화를 담당하므로 채널까지 채팅이면 어디를 봐야 하는지가 갈라진다"였다. 이 전제
   자체가 바뀌었다 — **장기 목표가 쿨메신저 폐지(3년 내)다.** 그러면 메시지 기능은 쿨메신저와
   중복되는 게 아니라 **이관 준비**다. 두 곳으로 갈라지는 기간은 과도기로 감수하고, 그 기간에
   교사들이 미리 경험하게 만드는 것이 오히려 목적이 된다.

   → 이 결정은 스코프를 크게 바꾼다. "업무 글에 대화를 곁들인다"가 아니라 **"쿨메신저를
   대체할 수 있는 메시징을 만든다"**가 되므로, DM·읽음·검색·알림이 전부 필수 요소가 된다.
   구체적인 모델은 아래 **"메시징 모델"** 절에 확정돼 있다.
3. **정기 프로세스 템플릿화 — 방향은 유지, 지금 착수 안 함 확정.** 학운위·부장회의·보안점검
   같은 건 나중에 차근차근 만들어가도 되는 영역. 이번 작업계획 범위에서 제외
4. **공개/비공개 — Slack과 동일하게 "존재 자체를 숨기는" 완전 비공개로 확정.** 특수교육처럼
   개인정보 민감도가 높은 채널이 실제로 있다는 걸 데이터로 확인했기 때문(위 "본문까지 확인한
   2차 분석" 참고). 8/2에 세웠던 "채널 이름은 항상 보여야 참여 요청을 꺼낼 수 있다"는 설계
   철학과 정면으로 충돌하는데, **완전 비공개 채널에 한해 예외**로 두는 방향 — 일반 채널은
   기존 방식(이름 공개, 내용만 제한) 그대로, 비공개로 지정한 채널만 존재 자체를 숨긴다

### D. 스코프 제외 (2026-08-24 저녁 확정, 이모지 리액션은 재검토 후 채용으로 이동)

| Slack 기능 | 결정 |
|---|---|
| 에이전트 및 앱(플러그인 마켓) | 제외 확정. 구글드라이브·스마트교무실 등 정적 외부 링크만 (B 참고) |
| 허들(음성/화상 통화) | 제외 확정. 학교는 이미 물리적으로 같은 공간, 전화·대면이 대체 |
| 워크플로 빌더(자동화 앱) | 제외 확정. "업무 리스트-워크플로 기능"으로 이미 장기 과제 |
| ~~이모지 리액션~~ | **→ 채용 확정으로 이동 (아래 참고)** |

**이모지 리액션 — 채용 확정 (2026-08-24 저녁)**: 처음엔 뭘 말하는지 몰라 스코프 제외했다가,
Slack 메시지 호버 툴바 스크린샷(✅ 완료됨 · 👀 · 🙌 · 이모지 추가 · 공유 · 더보기)을 보고
채용으로 확정. **✅(완료) 리액션은 지금 있는 `completedUids`(완료 체크) 개념과 그대로
겹친다** — 별도 리액션 시스템을 새로 만들기보다, 완료 체크의 UI 표현을 이 리액션 방식으로
가져가는 통합이 자연스러워 보인다(세부 설계는 미착수).

### E. 전부 채용 확정 (2026-08-24 저녁, 미결이었던 것 전원 확정)

- [x] **멘션(@사람, @channel)** — 반드시 필요. 지금 데스크톱 알림(이벤트 기반: 새 요청·쪽지·
      마감임박)과는 별도 트리거로 추가해야 함
- [x] **채널별 알림 끄기(mute)** — 필요
- [x] **전체 검색(Canvas + 메시지 모두)** — 필요. **접근 제어 필수**: 비공개 채널은 본인이
      소속된 경우에만 검색 결과에 노출되어야 한다 — C-4의 "존재 자체를 숨김" 원칙이 검색에도
      똑같이 적용되어야 함(안 그러면 검색으로 존재가 새어나감)
- [ ] 안내 4분류(상시/정기/비정기/1회성)를 채널 생성 시 메타데이터로 남길지 — 유일하게 남은
      미결 항목

---

## 메시징 모델 (2026-08-24 확정)

C-2에서 "메시지 + Canvas 하이브리드로 간다"고 정한 뒤, 그 메시지가 구체적으로 어떤 모양이어야
하는지를 정리한 결과다. **P0의 최대 쟁점이었던 "쪽지를 대화형 DM으로 바꿀 것인가"가 여기서
해소됐다.**

### 1. 채널 메시지는 멤버 전원이 읽는다 — 그게 존재 이유다

논의 중 "채널 메시지를 채널 주인만 읽게 해야 하나?"라는 질문이 나왔지만, **정반대다.**
쿨메신저 데이터에서 확인한 것이 정확히 이 지점이었다 — 같은 질문(감독교사 업무 절차, 누가기록
입력 여부 등)이 1:1 쪽지로 반복 유입되고 그때마다 같은 답을 다시 썼다. 채널에서 묻고 답하면
**한 번의 답이 전원에게 남는다.** "전달의 전달"을 없애는 지점이 여기다.

### 2. 다만 필요한 건 읽기 제한이 아니라 **쓰기 제한**이다

| 채널 종류 | 읽기 | 쓰기 |
|---|---|---|
| 일반 채널 | 멤버 전원 | 멤버 전원 (질문·응답) |
| **공지 전용 채널** | 멤버 전원 | **채널 주인만** |

부장회의 안내·보안점검처럼 일방 안내만 필요한 채널은 아무나 쓰면 안내가 대화에 묻힌다.
Slack에도 같은 옵션이 있다. (위 "정기 프로세스" 후보들이 대부분 여기 해당한다.)

### 3. 채널 우선, DM은 폴백

디렉터리에서 사람을 고를 때 **채널을 지정하면 그 채널의 공개 질문, 지정하지 않으면 1:1 DM**이
된다. 이 기본값 하나가 "질문은 되도록 채널에서" 문화를 구조로 유도한다.

### 4. **DM은 별도 시스템이 아니라 "이름 없는 2인 채널"이다** ★

Slack이 실제로 이렇게 만들어져 있다(Slack API에서도 DM은 `conversation` 타입 중 하나일 뿐,
별도 개체가 아니다). 우리도 같은 방식을 채용한다.

```
channels/{id}
  type: 'channel'  → 이름 있음, 멤버 다수
  type: 'dm'       → 이름 없음, 멤버 2명
```

이 결정의 값어치가 크다:
- 메시징 시스템을 **한 벌만** 만들고 유지한다
- 검색(P5)·멘션(P4)·알림·mute가 **자동으로 DM에도 적용**된다 — 두 번 만들지 않는다
- 비공개 채널 rules(P1)가 **DM 보안을 그대로 커버**한다 (DM = 멤버 2명짜리 비공개 채널)

### 5. 기존 쪽지(`personalNotices`)는 읽기 전용으로 동결

선택지는 ⑴ DM 채널로 마이그레이션 ⑵ 읽기 전용 동결 ⑶ 그대로 병존 셋이었고, **⑵로 확정**했다.
과거 쪽지는 그대로 두고 신규 대화만 DM으로 받는다 — 마이그레이션 리스크 없이 갈아탈 수 있고,
과거 기록도 잃지 않는다.

### 정정 — "쪽지=포인터 원칙이 무너진다"는 앞선 판단은 과장이었다

작업계획 초안에서 "쪽지를 대화형으로 바꾸면 '쪽지=포인터, 업무글=캔버스' 원칙과 충돌한다"고
적었으나 틀렸다. 그 원칙은 **내용이 어디에 사는가(캔버스)**에 관한 것이지 메시지가 대화형인지와는
무관하다. 대화형 DM에서도 "이거 확인해주세요 [링크]"는 그대로 성립한다. 오히려 "채널 우선,
DM 폴백"(위 3)이 원칙을 강화한다 — 질문이 채널로 가면 답이 저절로 축적되기 때문이다.

---

## 현재 구조 (코드로 확인됨, 2026-08-24)

- **`requests`(업무 글: 요청·안내)**: `targetRule.conditions`(부서/교과/직급/수업학년)를
  `targeting.js`가 풀어 `targetUids`(대상자 배열)로 채운다. 개인은 `completedUids`에 자기
  uid를 넣고 빼며 완료 체크. **"이 일은 OO 담당"처럼 단일 책임자를 지정하는 필드는 없다** —
  담당자는 항상 "조건에 해당하는 그룹 전체"다.
- **`channels`**: `requests.channelId`로 글에 붙는 이름표일 뿐, 별도 글 컬렉션이 아니다
  (`apps/shared/lib/channels.js`). 채널 멤버십도 `memberRule`로 같은 조건 엔진을 공유한다.
  댓글(`comments`)·보관(`archived`)·나가기(`leftUids`)·참여자 갱신 감지(`memberDiff`)까지
  구현·검증 완료(2026-08-02, 테스트 104개, 브라우저 종단 확인).
- **채널 Read 권한은 항상 "소속 교사 전체"로 고정**돼 있다(`firestore.rules:241`). 참여자만
  읽게 하면 채널 이름조차 못 봐서 "넣어달라"는 말을 꺼낼 수 없다는 이유로 8/2에 의도적으로
  이렇게 설계했다. → **지금 채널은 전부 사실상 "공개"다. 비공개 개념이 아예 없다.**
- **`personalNotices`(쪽지)**: `title`/`bodyHtml`/`content`/`attachments`뿐이고, **채널이나
  업무 글을 참조하는 필드가 없다.** 100% 자유 텍스트.

---

## Slack 관찰 → 적용

### 1. DM은 채널에 편입되는 게 아니라, 메시지/Canvas를 "공유"로 채널에 링크한다

Slack의 채널 안 문서(Canvas)는 우리 시스템의 `requests`(채널에 딸린 요청·안내 글, 블록
에디터로 쓴 서식 본문)와 정확히 같은 역할이다. 새 개념이 필요 없다.

**도출된 원칙:**

> 쪽지 = 포인터/알림 ("이거 확인해주세요"), 채널의 업무 글(`requests`) = 캔버스(실제 내용의
> 단일 진실원)

지금 쪽지는 이 역할을 못 한다 — 참조 필드가 없어서 "구구절절 쓰지 말고 캔버스를 가리켜라"가
구조적으로 불가능하다. **쪽지에 `requestId`(또는 `channelId`) 참조 필드 하나만 추가하면 되는
배관 문제**로 정리됐다. 채널을 몇 개로 쪼갤지(아래 참고)와는 독립적이라 데이터 없이도 설계
가능하다.

### 2. 사이드바 구조 (Slack 데스크톱 앱 스크린샷 참고, 2026-08-24)

`허들/디렉터리` → **즐겨찾기** → **채널**(사용자가 일부를 골라 섹션으로 묶어 관리 가능) →
**다이렉트 메시지** → **에이전트 및 앱**(Slack 앱스토어급 — 스코프 아님, 대신 구글 드라이브·
스마트교무실 등 외부 링크만 가볍게 보여주는 정도가 적당하다고 판단).

**지금 코드에 없는 것 (신규 기능 후보):**

| 기능 | 현재 상태 | 비고 |
|---|---|---|
| 채널 공개/비공개 옵션 | 없음 — Read가 무조건 전체 공개로 고정 | firestore.rules를 조건부로 나눠야 함. 기존 "채널 이름은 항상 보여야 한다"는 설계 철학과 어떻게 공존시킬지 설계 필요 |
| 섹션(채널을 사용자가 임의로 그룹핑) | 없음 — `Channels.jsx` 사이드바는 flat list + 보관함/나간채널 collapse뿐 | `users/{uid}`에 이미 `dashboardLayout` 등 개인화 상태를 저장하는 패턴이 있어 재사용 가능해 보임 |
| 즐겨찾기(핀) | 없음 | 위와 같은 패턴으로 개인화 저장 가능 |
| 홈 화면 외부 링크(구글드라이브·스마트교무실 등) | 없음 | 가볍게 추가 가능한 기능 |
| 쪽지 → 업무글/채널 참조 필드 | 없음 (`personalNotices.js` 확인됨) | 위 "도출된 원칙" 참고 |
| 업무 리스트 - 워크플로 | 없음 | **장기 과제로 명시적으로 스코프 제외** (2026-08-24 확인) |

---

## 업무분장 데이터로 본 "채널 세분화 단위" 문제

**원본**: `C:\Users\USER\Downloads\2026학년도 업무분장.hwpx` (2026.3.1. 기준, 전체 교원 50명
— 교장·교감 포함). `hwpx-parser`(`C:\Claude code\hwpx-parser\lib.js`의 `loadTables` — adm-zip으로
압축 해제 후 `Contents/section*.xml`에서 `hp:tbl` 추출)로 표 파싱 성공.

**구조**: `구분(대분류) → 부서(중분류) → 성명 → 담임(비/1담/2담/3담) → 업무(짧은 직함) →
주요 업무 내용(상세, 콤마로 나열)`

**발견한 문제**: 한 사람이 업무명 하나 아래 **10~20개의 서로 다른 일을 뭉쳐서 갖고 있다**
(예: "교무기획" 담당 1명 아래 수능 업무·입학식 안내책자·진급사정·규정관리·공문서관리·
청렴교육·교권보호·원격수업·e-알리미·랑톡·학부모회·학사달력·학부모총회·교직원현황·
방학교육계획·일과시간표 등 16개). **이걸 다 채널화해야 하는가?**

그 외 관찰:
- **정·부 이원 구조**가 곳곳에 있다(예: "고사1"=정기고사 정담당, "고사2"=부담당). 인수인계
  설계에 힌트가 될 수 있다 — 부담당이 다음 해 정담당이 되는 경로가 조직 구조에 이미 있다.
- **담임(비/1담/2담/3담) 축은 부서 업무 축과 별개**다. 학년부는 두 축이 겹치지만(예:
  "학생부/출결" = 해당 학년 담임), 일반 부서는 담임 여부와 무관하게 업무가 배정된다. 채널을
  "부서 채널"과 "학년 채널"로 나눌 때 이 둘이 다른 축이라는 걸 구분해야 한다.

### 안내 4분류로 세분화 단위 문제를 풀다 (사용자 정리, 2026-08-24)

학교 업무를 **안내 성격**으로 나누면:

1. **상시안내** — 1년간 비슷한 내용을 여러 번 반복 안내
   (학사일정 변경, 일과운영 시간 변경, 창체 자율 시간 운영 안내, 보강 신청 안내)
2. **정기안내** — 학사일정에 사전에 정해진 업무를 안내
   (정기시험(중간·기말) 안내 - 시험계획·감독교사 배정표 등, 학력평가 시감 안내,
   교사 개인별·반별 시간표 안내)
3. **비정기안내** — 학사일정에 없던 업무가 진행될 때
4. **1회성안내** — 업무와 관련성은 낮지만 안내는 필요
   (상조회 안내, 경조사 안내, 인사 관련 안내 등)

**채널 전략 매핑:**
- 상시안내·정기안내 → 지속되는 주제라 **채널에 정확히 대응**
- 비정기안내 → 발생 시점에 **임시 채널을 만들고, 끝나면 보관**(archived, 이미 있는 기능
  그대로 재사용)
- 1회성안내 → **채널이 불필요할 가능성이 높다.** 상조회·경조사를 채널로 만들 이유가 없다 —
  기존 `announcements`(전체 공지) 쪽이 더 맞을 것으로 보인다

### 실제 규모 감 (부서별 예시, 사용자 정리)

모든 교사가 업무분장상 담당은 갖고 있지만, **"채널로 안내할 굵직한 업무"는 비담임 일부
교사 + 부서당 2~4개 정도뿐**이다.

- 교무기획부: 정기시험 안내 / 생활기록부 작성 안내 / 최소성취수준 보장지도 안내
- 연구부: 교육계획서 작성(학기초) / 학교평가 운영(2학기) / 신학년 집중준비연수
- 창의체험부: 축제 / 매주 창체 운영 안내

→ **결론(합의): 실제 생성되는 채널 수는 많지 않을 것이다.** 비공개 채널이 일부 섞여도
문제없다고 판단.

---

## 쿨메신저 실데이터로 본 검증 (2026-08-24)

`coolmsg_2026_08_24.15.25.49.xls`(2026-02-10 ~ 08-24, 받음 1,818건 · 보냄 765건)를 분석했다.
**발신자·수신자 컬럼은 아예 읽지 않았고**, 업무분장 원본에서 확인된 실제 교직원 이름이 제목에
등장한 메시지(서명·전달 표시 등)는 통계에서 완전히 제외했다 — 상세 데이터와 방법은
`업무분석_작성양식.xlsx`의 "쿨메신저 데이터 근거" 시트 참고(다운로드 폴더에 있음, 실명 없음).

**월별 건수가 학사일정과 정확히 맞물린다**: 3월(받음 432·보냄 178, 합계 610)이 압도적 1위,
이후 4월(444) → 5월(249) → 6월(406, 기말 시기 재상승) → 7월(252) → 8월(진행중, 141). 신학기
초(3월)에 안내가 집중된다는 게 정량으로 확인됨 — "정기안내"가 학사일정에 진짜로 종속돼 있다는
가설을 뒷받침.

**반복 주제 top에서 4분류 후보가 실제로 나왔다**:
- "부장회의 안내"류 — 13회, 6개월 내내 지속 → **상시안내**의 강력한 실례
- "전입생/자퇴생/전출생 안내"(순번·학번 포함 명단 통지) — 각각 10회/8회/5회, 3~4개월 지속 →
  학사일정에 못 박혀 있진 않지만 반복되는 패턴 → 지금 4분류의 "비정기안내"와는 결이 다른,
  **"이벤트 발생형 반복 안내"라는 다섯 번째 결이 있을 수도** (미해결 — 아래 참고)
- "보강 신청 부탁드립니다"류 — 여러 클러스터 합쳐 10회 내외 → 이미 구현된 `/cover/*` 보강
  신청 시스템과 실사용 패턴이 정확히 일치. 검증 완료로 봐도 됨
- "직무연수 공람" — 9+4=13회, 2개월 집중 → 상시안내 후보
- "학교운영위원회 안건 제출 요청" — 3회, 6개월에 걸쳐 지속 → 정기안내 후보(회의 주기형)
- 상위 키워드에도 중간고사·기말고사·수행평가·학업성적관리위원회·보강·출결·예산·결재·
  교육활동계획이 상위권 — 지금까지 "정기안내"로 분류해온 항목들과 일치

**새로 발견된 것**: 학생 신상 변동 안내(전입/전출/자퇴)가 예상보다 훨씬 잦고 반복적이다.
업무분장 표에는 "학적"(교육실무사 박밀양) 담당으로만 나와 있어 이 정도 빈도인 줄 몰랐을 수
있다 — 채널 후보에 추가할 가치가 있어 보임.

**한계**: 제목만 분석했다(본문 내용은 길이만 썼고 텍스트는 보지 않음 — 이름 유출 위험을
줄이려고 보수적으로 접근함). 업무분장에 없는 사람(학생·학부모·외부기관·비담임 일반교사 등)의
이름이 제목에 남아있지 않은지는 육안으로 재확인했지만 100% 보장은 아니다. 더 정밀한 분류(예:
본문까지 읽어 4분류를 자동 태깅)가 필요해지면 이름 마스킹을 더 강화한 뒤 다시 시도해야 한다.

### 본문까지 확인한 2차 분석 (2026-08-24) — 실명 포함 로컬 분석, 이 문서엔 요약만

사용자가 "GitHub(이 리포)에만 안 들어가면 된다"고 확인해줘서, 본문 내용까지 실명 레닥션 없이
로컬 스크래치 파일로 전체 확인했다(그 파일 자체는 git에 없고 삭제됨). **이 문서에는 사람을
특정할 수 있는 내용은 넣지 않고 구조적 발견만 남긴다.**

- **작성자(사용자 본인)가 교무부장 — 즉 지금 학교 정보 유통의 최대 허브다.** 쿨메신저
  받은함/보낸함 볼륨이 압도적으로 큰 이유가 여기 있다. 채널 구조가 실제로 풀어야 하는 문제는
  "교무부장 개인이 전달자 역할을 떠안고 있는 것"에 가깝다 — 이 관찰이 "담당자는 어떻게
  연결되는가"라는 원래 질문에 실마리를 준다: 지금은 조직 전체가 아니라 **한 사람의 재량으로
  전달 여부가 결정**되고 있다
- **"전달의 전달" 체인이 실제로 관찰된다.** 메시지 본문 안에 `OOO님이 보낸글 >>` 인용이
  2~3단으로 중첩된 경우가 흔하다(관리자→부장→개별교사 순 릴레이). 채널이 있었다면 이 릴레이
  자체가 필요 없다 — 원 글이 채널에 한 번 올라가면 전원이 같은 곳을 본다. **채널의 존재
  이유를 실데이터로 확인한 셈**
- **"수정판 재공지" 패턴이 반복적으로 나타난다.** 계획을 한 번 안내한 뒤 일정이 바뀌면
  본문 전체를 다시 복사해 "(수정)"을 붙여 통째로 재발송하는 방식이 여러 주제(성적 처리 일정,
  운영 예정 안내 등)에서 보였다. **캔버스(채널에 딸린, 고쳐 쓸 수 있는 업무 글)가 정확히
  이 문제를 없앤다** — "쪽지=포인터, 업무글=캔버스" 원칙이 실제 반복 패턴으로 뒷받침됨
- **정·부 이원담당이 실무에서 그대로 작동 중이고, 재배정도 즉흥적으로 메시지로 일어난다.**
  업무분장의 "고사1/고사2" 정·부 구조가 실제 조정에도 쓰이고, 담당자를 바꾸는 결정 자체가
  쪽지 한 통으로 비공식적으로 이뤄지는 사례가 있었다. 채널에 "담당자(정/부)" 필드가 있으면
  이 재배정이 구조화된 이력으로 남는다 — 지금은 쪽지 더미에 묻혀 있다
- **완전히 템플릿화된 정기 프로세스 3종을 확인했다** — 자동화/캔버스 최우선 후보:
  1. **학교운영위원회**: 회차 번호(제189회~191회)가 붙는 진짜 정기 회의체. "안건 제출 요청 →
     마감 → 발표자·순서 확정 → 개최" 4단계가 매번 거의 동일한 문구로 반복된다
  2. **부장회의**: "OOO학년도 O월 O주차 부장회의 안내" 형식으로 주간 반복. 상시안내의
     교과서적 사례
  3. **보안점검(내PC지키미)**: 월 1회, 문구가 거의 완전히 동일하게 반복 발송된다. 채널조차
     필요 없고 자동 알림으로 대체 가능한 수준
- **민감 카테고리 존재를 실데이터로 확인** — 특수교육 관련 메시지 중 원 발신자가 스스로
  "개인정보 보호를 위해 메시지 내용이 외부에 유출되지 않도록 유의해주시기를 부탁드립니다"라고
  명시한 사례가 있었다. **채널 공개/비공개 옵션이 왜 필요한지 실증됨** — 특수교육처럼 접근을
  좁혀야 하는 채널이 실제로 존재한다
- **"예산/결재/공문"이 전체 주제 중 압도적 1위 볼륨(304건)이었다.** 이건 "업무 중심 채널"의
  단위라기보다 전자결재/공문 시스템의 알림 성격에 가까워 보인다 — 채널로 만들지, 아니면
  범위에서 아예 빼고 별도 취급할지는 미결

**교훈**: 처음 시도(제목만, 이름 레닥션)는 안전했지만 "왜 채널이 필요한가"에 대한 근거로는
얕았다. 본문까지 봐야 "전달 체인"·"재공지 패턴"·"즉흥 재배정" 같은, 채널이 실제로 없애는
비효율이 보인다. 다만 본문 데이터에는 학생 개인정보(학번·이름·특성 등)가 다량 섞여 있어
**이 원본 데이터 자체를 다른 사람과 공유하거나 리포에 커밋하는 일은 하지 않는다.**

### 정정 — "정·부 이원담당 = 인수인계 경로" 서술은 틀렸다 (2026-08-24)

위에서 "고사1/고사2 같은 정·부 구조가 있으면 인수인계 경로가 이미 있다는 뜻"이라고 썼는데,
사용자가 바로잡아줬다: **정·부는 같은 업무를 이중으로 커버하는 백업 구조가 아니라, 하나의 큰
업무(예: 고사 준비)를 세부 업무 단위로 나눠 각자 다른 부분을 맡는 분업 구조**다. "부담당이
정담당 업무를 지켜보다 인계받는" 경로는 성립하지 않는다.

오히려 이 구조는 **지금 스키마(channels + requests)와 그대로 맞아떨어진다** — 채널을 "고사
준비"라는 상위 프로젝트로 만들고, 그 안의 세부 업무 각각을 별도 `request`로 쪼개 정/부 각자
다른 담당자를 지정하면 된다. 채널 멤버십(누가 이 프로젝트를 보는가)과 개별 요청의 대상(누가
이 세부 업무를 하는가)이 이미 분리돼 있는 지금 구조 그대로 쓸 수 있다 — **새 필드가 필요한
게 아니라, 채널을 만들 때 "세부 업무 단위로 쪼개 각각 request를 만들라"는 사용 관행/가이드가
필요한 것**에 가깝다.

---

## 채널 시스템의 목적·가치·설치 방향 (2026-08-24 정리)

지금까지 나온 걸 한 방향으로 정리하면:

**목적 — 쿨메신저의 구조적 낭비 3가지를 없앤다**
1. 전달의 전달 체인 — 정보가 한 사람(들)을 거쳐야만 다음 사람에게 전달됨
2. 재공지 시 전체 재작성 — 수정할 때마다 본문을 통째로 복붙해 다시 보냄, 어느 게 최신본인지
   구분이 안 됨
3. 담당자 재배정이 구조화되지 않고 쪽지 한 통으로 비공식적으로 일어나 이력이 안 남음

**가치 — 수혜자는 허브 한 명이 아니라 전체 교직원**

사용자 본인(교무부장)이 정보 허브라 쿨메신저 볼륨이 컸던 건 맞지만, **전달 체인은 아래로
갈수록(관리자→부장→개별교사) 같은 정보가 여러 경로로 중복 도달할 가능성이 커지는 구조**다.
즉 낭비는 허브에서 끝나는 게 아니라 조직 전체로 퍼진다 — 이번 분석은 사용자 한 사람의 계정만
봤기 때문에 다른 교사의 실제 수신량을 데이터로 검증하지는 못했지만, 전달 체인의 구조상 합리적
추론이다. 정기 프로세스(학운위·부장회의·보안점검)는 이미 패턴이 완전히 굳어 있어 가장 낮은
비용으로 가장 먼저 효과를 볼 수 있는 영역이다.

**설치 방향 — 이미 반복이 증명된 것부터, 만드는 사람이 먼저 써본다**
- 전 교직원 강제 도입이 아니라, **데이터로 반복이 확인된 업무부터** 파일럿한다(학운위·
  부장회의·보강신청처럼 패턴이 이미 뚜렷한 것)
- 교무부장(사용자)이 최대 병목이자 최대 수혜자이므로, **본인이 관리하는 채널부터 먼저
  만들어 스스로 효과를 검증한 뒤 다른 부서로 확산**하는 순서가 자연스럽다
- 채널(프로젝트 단위) 안에서 세부 업무는 개별 request로 쪼개고 각각 다른 담당자를
  지정한다 — 정·부 분업 구조와 정확히 맞아떨어지며 스키마 변경이 필요 없다

---

## 작업계획 (2026-08-24 수립)

### 먼저 — 계획 전체를 바꾸는 발견 3가지

계획을 세우려고 현재 코드를 확인하다 **작업 순서를 결정해버리는 제약**을 찾았다.
기능 목록 순서대로 만들면 나중에 전부 갈아엎어야 한다.

**발견 1 — 비공개 채널은 기능 추가가 아니라 접근 모델 전복이다.**

지금 시스템의 근간 가정은 "학교 안에서는 전부 열려 있다"이다.
- `firestore.rules`: 채널도 업무 글도 `allow read: if isTeacher(schoolId)` — **모든 교사가
  모든 것을 읽는다**
- `useChannels.js:49`: `query(collection(db, ...schoolPath(schoolId, COL.REQUESTS)))` —
  필터 없이 **학교 전체 업무 글을 통째로 구독**한다(교직원 60명·글 수백 건 규모라 그게 싸다는
  판단이 주석에 명시돼 있다)

여기에 비공개 채널을 넣으면 세 곳이 동시에 깨진다: ① 채널 문서 read 제한 ② **그 채널에 속한
업무 글(`requests`)도 함께 제한** — 안 그러면 채널만 숨기고 내용은 그대로 읽힌다 ③ 위의 "전체
구독" 쿼리가 **permission-denied로 통째로 실패**한다(Firestore는 쿼리 결과 중 하나라도 못 읽으면
쿼리 전체가 실패한다. 일부만 걸러 주지 않는다).

→ **비공개 채널을 나중에 하면 그때 데이터 접근 코드를 전부 다시 짜야 한다. 가장 먼저 해야 한다.**

**발견 2 — 메시지는 새 컬렉션이고, 쿨메신저 폐지 목표가 스코프를 키운다. (해소됨)**

지금 채널에 붙는 건 `requests`뿐이다. 대화를 담을 그릇이 없다. 그런데 목표가 "채널에 대화 좀
곁들이기"가 아니라 **쿨메신저 대체**라면, 채널 메시지만으로는 부족하다 — 쿨메신저 트래픽의
상당량이 1:1 대화이기 때문이다(이번 분석에서 확인된 "전달의 전달" 체인이 전부 1:1 쪽지였다).

그런데 지금 1:1 자리를 `personalNotices`(쪽지)가 차지하고 있고, 그건 **대화가 아니라 "보내고
확인" 모델**이다(제목+본문+읽음 1건, 수신자마다 문서 분리 — `personalNotices.js` 주석에 설계
의도가 명시돼 있다). 답장하면 `Re:` 제목의 새 문서가 생긴다. 이 구조로는 쿨메신저를 대체할 수 없다.

→ **해소됨 (2026-08-24)**: "쪽지를 대화형으로 바꾼다"가 아니라 **DM을 "이름 없는 2인 채널"로
만들고(`type: 'dm'`), 기존 쪽지는 읽기 전용으로 동결**하는 방향으로 확정됐다. 메시징 시스템이
한 벌로 통일되면서 검색·멘션·알림·보안 규칙을 두 번 만들지 않아도 된다. 상세는 위
**"메시징 모델"** 절 참고.

**발견 3 — 검색은 Firestore만으로 안 된다.**

Firestore는 전문 검색(full-text search)을 지원하지 않는다. 지금까지는 데이터가 적어
클라이언트에서 통째로 받아 필터링하는 방식이 통했지만, 메시지가 들어오면 규모가 달라진다
(쿨메신저 실측: 한 사람이 반년에 2,500건 → 전 교직원이면 연 수십만 건). 게다가 비공개 채널
검색 제외까지 얹혀야 한다.

→ 검색은 별도 인프라 판단이 필요한 독립 과제다. 메시지가 쌓인 뒤 실측하고 정하는 게 맞다
(P5를 뒤로 미룬 이유).

---

### 단계별 계획

의존성 순서다. P1은 반드시 먼저, P6은 언제든 병렬 가능.

| | 단계 | 핵심 | 선행 |
|---|---|---|---|
| **P0** | 데이터 모델 설계 | 비공개 + 메시지 스키마를 **함께** 확정 | — |
| **P1** | 비공개 채널 · 접근 제어 | rules 재설계 + 데이터 구독 방식 교체 | P0 |
| **P2** | 채널 메시지 | 채널 안 대화 스트림 (P2a 완료 · DM은 P2b) | P1 |
| **P3** | Canvas 접근 루트 · 홈 재구성 | 채널 헤더 탭은 P2a에서 들어감. 남은 것은 캔버스 위상·빈 채널 첫인상·홈 통합 | P2 |
| **P4** | 리액션 · 멘션 · mute | 메시지 위에 얹히는 것들 | P2 |
| **P5** | 통합 검색 | 메시지 + Canvas, 비공개 제외 | P2, P4 |
| **P6** | 사이드바 개인화 | 즐겨찾기 · 섹션 · 외부 링크 | 없음(병렬) |
| **P7** | 쪽지 정리 | DM 통합 여부 결정 후 실행 | P2 |

**P0 — 데이터 모델 설계 (문서 작업, 코드 없음)** — ✅ **완료(2026-08-24)**,
산출물: **[PLAN_channels_datamodel.md](./PLAN_channels_datamodel.md)**

비공개와 메시지를 **한 번에** 설계한다. 따로 하면 `firestore.rules`를 두 번 갈아엎게 된다.

- 채널 공개 범위 필드 설계 (`visibility: 'public' | 'private'`)
- **채널 종류 필드** (`type: 'channel' | 'dm'`) + **쓰기 권한 필드**(일반 / 공지 전용) —
  "메시징 모델" 절의 확정 사항을 스키마로 옮긴다
- **비공개 채널의 업무 글 접근을 rules에서 어떻게 판정할지** — 핵심 난제.
  rules에서 `get()`으로 채널 문서를 매번 읽는 건 비용·지연이 크다. 대안은 `requests` 문서에
  멤버 목록을 비정규화해 복사하는 것(`channels.memberUids`를 이미 같은 이유로 비정규화하고
  있으므로 패턴이 일관됨). 대신 멤버가 바뀔 때 글 전체를 갱신해야 하는 비용이 생긴다 — 트레이드오프 판단 필요
- 메시지 컬렉션 구조 (`channels/{id}/messages/{id}` 위치, 읽음 처리 방식, 스레드 여부)
- DM 채널의 암묵적 생성 규칙 — 처음 메시지를 보낼 때 만드는지, 같은 상대와 중복 생성을 어떻게
  막는지(문서 ID를 두 uid 조합으로 고정하는 방식 등)
- 기존 채널 데이터 마이그레이션 계획(전부 `type: 'channel'`, `visibility: 'public'`으로 채우기)

**P1 — 비공개 채널 · 접근 제어**

- `firestore.rules` 재설계 — 채널 + requests 양쪽
- `useChannels.js`의 전체 구독 쿼리 교체 (발견 1의 ③)
- 채널 생성/수정 UI에 공개 범위 선택 추가
- **검증 필수**: 비공개 채널이 비멤버에게 ⑴목록 ⑵직접 URL ⑶검색(P5 이후) ⑷알림 어디에서도
  드러나지 않는지. "존재 자체를 숨긴다"는 결정이라 한 곳만 뚫려도 의미가 없다

**P2 — 채널 메시지 + DM**

- 메시지 전송·구독·표시. 읽음 처리
- **DM(`type: 'dm'`)을 같은 코드로 함께 구현** — 별도 시스템이 아니므로 추가 비용이 작다
- 디렉터리에서 사람 선택 → 채널 지정 시 채널 질문, 미지정 시 DM ("채널 우선, DM 폴백")
- 공지 전용 채널의 쓰기 제한 적용
- 채널 화면 재구성: 상단 메시지 + Canvas 분리 (P3와 함께 설계)
- 데스크톱 알림 파이프라인 연결 (`useDesktopNotifications.js`에 트리거 추가)

**P3 — Canvas 접근 루트 · 홈 화면 재구성** (2026-08-24 Slack 실화면 참고해 구체화)

P2a에서 채널 머리에 `대화 / 업무 글` 탭을 넣었는데, Slack 실화면을 보니 **같은 자리에 같은
구조**였다(`메시지 / 캔버스 추가 / ＋`). 방향은 맞았고, 남은 차이는 셋이다.

1. **캔버스가 탭 하나당 문서 하나다.** Slack은 `＋`로 캔버스를 여러 개 붙이고 각각이 탭이
   된다. 우리는 `업무 글` 탭 하나 안에 글 목록이 들어 있다. 어느 쪽이 맞는지는 실사용에
   달렸다 — 채널당 상시 문서가 한둘이면(예: "고사 운영 절차") Slack 쪽이 낫고, 마감이 걸린
   글이 계속 쌓이면 목록이 낫다. **업무 분석 데이터가 나온 뒤 판단한다**
2. **빈 채널의 첫인상.** Slack은 `# 채널명에 모두가 있습니다` 큰 제목 + 설명 + 다음에 할
   일 카드(핸드북 추가·팀원 초대)를 깔아준다. 우리 빈 화면은 한 줄짜리 안내뿐이라, 채널을
   막 만든 사람이 무엇부터 해야 할지 모른다. **비용 대비 효과가 큰 편**
3. **머리 오른쪽 도구줄** — 참여자 수·검색·더보기가 아이콘으로 나란히 있다. 우리는 참여자
   수가 부제 텍스트에 섞여 있고 검색은 아직 없다(P5).

**홈 화면 재구성 (사용자 요청, 2026-08-24)**: "홈 화면에 채널이 기본으로 들어가고, 채널 안에서
메시지·캔버스가 표시되는" 구조. 지금은 홈(요청받은 일·전체 공지·학사일정)과 채널이 레일에서
갈라진 별개 화면인데, Slack은 홈 자체가 채널 목록이다.

이건 P3보다 큰 변경이라 순서를 나눠 본다:
- 먼저 P3(채널 안 구조)를 다듬고, 홈 통합은 그 다음이다. 채널 화면이 아직 자리를 못 잡았는데
  홈까지 옮기면 되돌릴 곳이 두 배가 된다
- 홈을 채널 목록으로 바꾸면 **지금 홈에 있는 것들(요청받은 일·학사일정)의 자리가 문제**가
  된다. Slack에는 대응물이 없어 그대로 베낄 수 없는 지점이다 — 사이드바 상단 고정 항목으로
  남길지, 별도 탭으로 뺄지 정해야 한다

**P4 — 리액션 · 멘션 · mute**
- 리액션: **✅ 완료 리액션과 기존 `completedUids`를 통합할지 판단** (겹치는 개념)
- 멘션: `@사람`, `@channel` + 알림 트리거 신설
- mute: 채널별 알림 끄기 — 개인 설정이므로 P6의 개인화 저장과 같은 자리

**P5 — 통합 검색** — 발견 3 참고. 인프라 선택(클라이언트 필터 / Cloud Function / 외부 검색
서비스)을 실측 후 결정.

**P6 — 사이드바 개인화 (병렬 가능)** — 즐겨찾기·섹션·홈 외부 링크. 다른 단계와 의존성이 없고
`users/{uid}` 개인 문서만 건드리므로 언제든 끼워넣을 수 있다. **가장 빨리 체감되는 개선**이라
P1이 길어질 때 먼저 내보낼 수 있는 카드.

**P7 — 쪽지 동결** — `personalNotices`를 읽기 전용으로 전환한다. 신규 작성 진입점을 막고
과거 쪽지는 열람만 가능하게 남긴다. 마이그레이션은 하지 않는다("메시징 모델" 5 참고).
**P2에서 DM이 실제로 쓸 만하다고 확인된 뒤에 닫는다** — 순서가 뒤집히면 대체재 없이 길을
막는 셈이 된다.

---

### 이번 계획에서 뺀 것

- **정기 프로세스 템플릿화** — 방향은 유효, 지금 착수 안 함(C-3 확정)
- **워크플로 빌더, 에이전트/앱, 허들** — 스코프 제외 확정(D)
- **채널 실제 개설·운영** — 이번 단계는 구조 설계다. 어떤 채널을 먼저 만들지는 업무 분석
  데이터가 나온 뒤 판단

---

## 아직 정하지 않은 것

기능 채용 여부는 위 A~E에서 전부 확정됐다. 남은 건 **설계 판단**과 **데이터 대기** 둘뿐이다.

**P0 — 전부 해소됨 (2026-08-24)** → [PLAN_channels_datamodel.md](./PLAN_channels_datamodel.md)
- [x] ~~쪽지를 대화형 DM으로 바꿀 것인가~~ — DM은 "이름 없는 2인 채널", 기존 쪽지는 읽기 전용 동결
- [x] ~~비공개 채널의 업무 글 접근 판정 방식~~ — **비정규화 확정**. 규칙 `get()`은 목록 쿼리에
      쓸 수 없다(횟수 제한 + 쿼리 안전성 증명 불가). 데이터모델 §1·§3
- [x] ~~메시지 컬렉션 구조~~ — 채널 **하위 컬렉션**(경로에 channelId가 고정돼 `get()` 1회로 끝남),
      읽음은 마커 방식, 스레드는 v1 제외. §6·§7
- [x] ~~DM 중복 생성 방지~~ — 두 uid를 정렬해 만든 결정적 문서 ID. §8

**P1 착수 전 판단 필요 — 해소됨**
- [x] ~~관리자가 비공개 채널을 볼 수 있는가~~ — **확정(2026-08-24)**: 슈퍼 관리자는 못 봄,
      학교 관리자는 볼 수 있음. 단 **DM은 학교 관리자도 못 본다** — DM을 "2인 비공개 채널"로
      통합한 탓에 그대로 두면 관리자가 전 교직원의 1:1 대화를 열람하게 되기 때문(현행 쪽지
      규칙도 학교 관리자를 막고 있다). 데이터모델 §10

**나중에 판단해도 되는 것**
- [ ] ✅ 완료 리액션과 기존 `completedUids`를 통합할지 (P4)
- [ ] 검색 인프라 선택 — 메시지가 쌓인 뒤 실측하고 결정 (P5)
- [ ] 안내 4분류를 채널 생성 시 메타데이터로 남길지(필터링·자동보관에 쓸지)
- [ ] "이벤트 발생형 반복 안내"(전입생/자퇴생/전출생처럼 학사일정엔 없지만 반복 패턴은
      뚜렷한 것)를 4분류 중 어디에 넣을지, 다섯 번째 분류가 필요한지

**데이터 대기**
- [ ] **업무 분석 데이터** — 사용자가 직접 작성 예정(Claude가 대신 생성하지 않음).
      이 데이터가 나와야 채널 세분화 최종 단위, 담당자 연결 방식, **인수인계(업무 이관)
      시스템** 설계를 확정할 수 있다. 지금의 "안내 4분류"는 그 전까지의 잠정 프레임이다.
      **작업계획 P0~P7은 이 데이터 없이도 진행 가능하다** — 구조 설계와 채널 개설은 별개다
