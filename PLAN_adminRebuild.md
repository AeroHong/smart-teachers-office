# 관리자 페이지 전면 리빌드 — 신학기 준비 흐름 중심으로

> 작성일: 2026-07-30
> 상태: 논의 완료 / 코드 미착수
> 배경: 관리자 기능이 늘어나면서 `Admin.jsx`가 1,776줄 · 탭 7개까지 비대해졌고,
>       신학기 준비처럼 여러 탭을 왕복해야 하는 작업이 특히 불편해짐

---

## 1. 현재 관리자 영역 지도

```
/admin (AdminHub)  ── 메뉴 4개짜리 허브
 ├─ /admin/users ─────── Admin.jsx (1,776줄, 탭 7개)
 │    ├ 승인 대기      ├ 구성원 목록   ├ 사전 등록  ├ 학생 명단
 │    └ 교원 배정      ├ 호출 시스템   └ 학교 설정
 ├─ /admin/asa-cutoffs ──── 분할점수 기준
 ├─ /admin/asa-checklist ── 성취평가제 과목·교사
 └─ /admin/training-presets ─ 연수 명단
```

허브에는 4개만 있는데 실무의 대부분은 `/admin/users` 안 탭 7개에 몰려 있어,
허브가 허브 역할을 못 하고 있다.

## 2. 구조적 문제

**① 탭이 "무엇을 관리하나" 기준이라 "언제 하나"가 안 보인다.**
신학기에 사무실을 입력하려면 교원 배정 탭 → 자리 배치는 호출 시스템 탭 →
학생 OU 갱신은 학교 설정 탭. 하나의 연속된 작업이 세 곳에 흩어져 있다.

**② 어디까지 했는지 알 수 없다.**
부서 미배정이 몇 명인지, 담임이 다 찼는지, 자리 배치가 빠진 사무실이 있는지
집계가 없어 탭을 열어 눈으로 세야 한다.

**③ 선후 관계가 숨어 있다.**
교원 배정에 사무실 입력 → 호출 시스템에 사무실 목록 표시 → 자리 배치·기기 페어링 가능.
순서를 모르고 들어가면 "사무실이 없습니다" 안내만 만난다.
(여러 화면에 넣어둔 그 안내들은 사실 구조 문제의 증상이었다.)

**④ 연도 개념이 제각각이다.**
`teacherAssignments`·`officeLayouts`만 학년도를 갖고, `asaSubjects`는 없고,
`students.year`는 이력이 아니라 마지막 업로드 연도다. 무엇이 연도별이고 무엇이
덮어쓰기인지 관리자가 알 수 없다.

**⑤ `Admin.jsx` 1,776줄.** 탭을 하나 더 붙이는 방식은 한계.

## 3. 신학기 실제 순서와 어긋나는 지점

| 단계 | 하는 일 | 지금 위치 | 문제 |
|---|---|---|---|
| 1 | 전출입 확정 → Workspace 계정 정리 | 학교 콘솔 (시스템 밖) | — |
| 2 | 계정 동기화 | 학교 설정 | 학생 OU를 매년 수동 변경 |
| 3 | 관리자 권한 인수인계 | 구성원 목록 | 안내 없음 |
| 4 | 부서·직함·담당교과 | 교원 배정 | **전년도 복제 기능 없음** (스펙엔 있었음) |
| 5 | 담임 배정 | 교원 배정 | 미배정 학년/반 확인 불가 |
| 6 | 사무실 → 자리 배치 | 교원 배정 + 호출 시스템 | 두 곳 왕복 |
| 7 | 학생 진급·졸업·입학 | 학교 설정(OU) | **5장 참조 — 실제 문제 있음** |
| 8 | 도구별 설정(성취평가제 등) | 별도 페이지 3개 | 완전히 분리 |

## 4. 제안 구조

허브를 **신학기 준비 현황판**으로 바꾸고, 탭 대신 좌측 사이드바로 전환한다.

```
관리자
├─ 홈 ─── 학년도 선택 + 준비 현황 카드 + 해야 할 일
│         "부서 미배정 12명 · 담임 3개 반 비어있음 · 자리배치 2개 사무실 미완료"
├─ 계정 ──── 승인 대기 / 구성원 / 사전 등록 / Workspace 동기화
├─ 교직원 ── 교원 배정 (부서·직함·교과·담임) + 전년도 복제
├─ 학생 ──── 학생 명단 + 진급·졸업 처리
├─ 공간 ──── 사무실·자리 배치 + 호출 기기
└─ 도구 ──── 성취평가제 / 분할점수 / 연수 (기존 3개 페이지 흡수)
```

핵심은 **홈이 "지금 뭘 해야 하는지" 알려주는 것**.
마법사로 순서를 강제하면 학기 중엔 방해가 되므로, 체크리스트로 안내하되
아무 항목이나 바로 갈 수 있게 한다.

**"학년도 전환" 액션**을 홈에 둔다 — 전년도 배정 복제, 학생 OU 갱신,
졸업 처리를 한 번에 안내하는 진입점.

## 5. 논의 중 발견한 실제 문제 (리빌드와 함께 처리 필요)

### ① 학생 동기화가 나간 학생을 지우지 않는다
`functions/workspaceSync.js`의 `syncStaff`는 교원 OU에서 사라진 사람의
`preApproved` 문서를 정리하지만, `syncStudents`에는 그 로직이 없다.
현재는 증상이 없으나 **OU를 "학생 2027"로 바꾸는 순간 2026년 학생 문서가
전부 남아 명단이 두 배가 된다.** 학년도 전환 기능과 함께 처리해야 함.

### ② 학번이 재사용된다
`10101` = 1학년 1반 1번이라 내년 신입생도 같은 번호를 받는다.
문서 ID가 학번(`students/{studentId}`)이므로 **새 학생이 옛 학생 문서를 덮어쓴다.**
출결 로그에는 이름·학년·반·번호 스냅샷이 남아 과거 기록 읽기는 되지만,
학번으로 조회하면 서로 다른 사람이 섞인다.
→ 졸업생 아카이브 방식을 정해야 함 (예: `archivedStudents/{year}/{studentId}`).

## 6. 착수 전 결정 사항

- **졸업생 데이터**: 삭제 vs 별도 보관. 출결·상담 이력 보존 의무 여부에 따라 갈림
- **학년도 전환 시점**: 관리자가 버튼으로 명시적 전환 vs 3월 1일 자동
  (전년도 데이터를 언제까지 조회해야 하는지와 연결)
- **도구 페이지 흡수 범위**: 성취평가제·연수까지 새 구조로 끌어올지, 링크만 걸지
  (셋 다 운영 중이라 건드리면 회귀 위험)
- **권한 세분화**: 지금은 admin이면 전부 보임. "교무부장은 업무 분장만 /
  정보부장은 기기만" 처럼 나눌 필요가 있는지

## 7. 권장 착수 순서

1. **홈 현황판 + 학년도 전환** ← 지금 아예 없는 기능이면서 신학기마다 반복되는
   고통을 직접 줄여준다. 임팩트 최대
2. 5장의 실제 문제 2개 처리 (학생 정리 누락, 졸업생 아카이브)
3. 사이드바 구조로 전환 + `Admin.jsx` 분해 (기능 이동이라 회귀 위험 관리가 핵심)
4. 도구 페이지 흡수 (선택)

3번은 "옮기는 작업"이라 새 가치는 적고 회귀 위험은 있으므로, 1·2번을 먼저
끝내 효용을 확보한 뒤 진행하는 편이 안전하다.
