# 교내 메신저 — 쿨메신저 대체 검토

> 작성일: 2026-07-30
> 상태: 계획만 (미착수)
> 선행 조건: Electron 데스크톱 클라이언트 (PLAN 별도 — 재실 자동감지와 같은 클라이언트)

---

## 결론 먼저

**기술적으로는 충분히 가능하고, 기반의 상당 부분이 이미 만들어져 있다.**
남은 변수는 코드가 아니라 **운영 부담**이다. 메신저는 "있으면 좋은 도구"가 아니라
"안 되면 학교 소통이 멈추는" 인프라라서, 지금까지 만든 기능들과 성격이 다르다.

그래서 이 문서는 기능 목록보다 **단계별로 멈출 수 있게 쪼개는 것**에 초점을 둔다.
각 단계가 그 자체로 쓸모 있어야, 중간에 "여기까지만" 하고 멈춰도 손해가 없다.

---

## 1. 이미 갖춰진 것 (새로 안 만들어도 되는 부분)

메신저를 만들 때 보통 가장 귀찮은 것들이 이미 있다:

| 필요한 것 | 현재 상태 |
|---|---|
| 계정·인증 | Firebase Auth + Google 로그인, Workspace 자동 동기화 |
| 조직도 | `teacherAssignments`의 부서·직책·사무실 (수신자 선택 UI의 재료) |
| 접속 상태 | `presence` 컬렉션 (재실/수업 중/자리 비움) — 이미 구현 |
| 멀티테넌시 | 모든 컬렉션이 `schools/{schoolId}` 스코프 |
| 권한 체계 | `firestore.rules` 헬퍼(`isTeacher`, `isSchoolAdmin` 등) 정착 |
| 실시간 동기화 | Firestore `onSnapshot` — 이미 호출 시스템에서 검증됨 |
| 오프라인 대응 | Firestore 오프라인 persistence 내장 |
| 상시 실행·알림 | Electron 클라이언트 (재실 감지 목적으로 어차피 필요) |

체감상 메신저 작업량의 **3~4할은 이미 끝나 있다**고 봐도 된다.

## 2. 새로 만들어야 하는 것

### 데이터 모델 (초안)

```
schools/{schoolId}/conversations/{conversationId}
  type: "direct" | "group" | "department"
  memberUids: [uid]              // 보안 규칙이 이 배열로 접근 판정
  title: string                  // group/department만
  lastMessage: { text, senderUid, senderName, at }   // 목록 화면용 비정규화
  updatedAt

schools/{schoolId}/conversations/{id}/messages/{messageId}
  senderUid, senderName          // 이름은 발신 시점 스냅샷
  text, attachments: [{name, path, size}]
  createdAt
  deletedAt: null | timestamp    // 소프트 삭제

schools/{schoolId}/conversations/{id}/readState/{uid}
  lastReadAt                     // 안읽음 계산 기준
```

### 기술적으로 신경 써야 할 지점

- **안 읽음 개수** — 메신저에서 제일 까다로운 부분. `lastReadAt` 이후 메시지를
  세는 방식은 대화가 많아지면 읽기 비용이 커진다. Cloud Function 트리거로
  카운터를 유지하는 방식과 비교해서 결정 필요.
- **보안 규칙 비용** — 메시지 접근 판정에 부모 `conversations` 문서를 `get()` 해야 해서
  규칙 평가마다 읽기가 1회 발생한다. 일반적인 패턴이지만 비용 계산에 포함해야 함.
- **검색** — Firestore는 전문 검색이 안 된다. 최근 대화 내 검색은 클라이언트에서,
  전체 검색이 필요해지면 Typesense/Algolia 같은 외부 색인 필요.
- **파일 전송** — Firebase Storage + 규칙. 용량 제한·확장자 제한 정책 필요.
- **알림** — 데스크톱은 Electron 네이티브 알림, 모바일/웹은 FCM
  (FCM은 아직 이 저장소에 전혀 없음 — 처음부터 구축).

### 비용

교직원 60명 규모 한 학교면 Firestore 비용은 사실상 무시할 수준이다.
문제가 되는 건 학교 수가 늘었을 때의 **리스너 fan-out**이므로,
"열려 있는 대화만 구독 + 나머지는 요약만" 구조를 처음부터 지켜야 한다.

## 3. 진짜 어려운 부분 (코드 아님)

솔직하게 짚어야 할 것들:

1. **가동률 기대치가 달라진다.** 지금 만든 기능들은 몇 시간 멈춰도 "불편한" 수준이지만,
   메신저는 멈추면 그 시간 동안 학교 소통이 마비된다. 장애 시 연락받을 사람은 결국 본인.
2. **알림이 안 오면 그건 버그가 아니라 사고다.** Windows 알림, 절전 모드, 방화벽 등
   환경 변수가 많고 "제 컴퓨터만 안 와요" 문의가 반드시 발생한다.
3. **개인정보 부담이 커진다.** 교직원 간 대화 내용은 민감 정보다. 보관 기간, 퇴직자
   처리, 열람 권한(관리자가 남의 대화를 볼 수 있는가?), 감사 로그 정책이 필요하다.
   저장소에 이미 개인정보처리방침이 있으니 개정도 필요.
4. **전환 비용.** 쿨메신저는 이미 모두가 쓰고 있다. 기능이 조금 나은 정도로는
   안 옮겨간다. "이것 때문에 옮길 만하다" 싶은 한 가지가 있어야 한다
   — 예: 업무 대시보드·호출·학사일정과 한 화면에 붙어 있다는 통합성.

## 4. 단계 계획 (각 단계에서 멈출 수 있게)

### 0단계 — Electron 클라이언트 (선행, 이미 계획됨)
재실 자동감지 목적. 트레이 상주·네이티브 알림·자동 실행 기반이 여기서 만들어진다.
**메신저를 안 하더라도 어차피 필요한 작업.**

### 1단계 — 쪽지 (비실시간, 단방향)
- 조직도에서 수신자 선택 → 제목+본문 전송, 읽음 표시
- 실시간 채팅이 아니라 "보내고 확인하는" 모델이라 난이도·리스크가 훨씬 낮다
- **여기서 실제 사용률을 본다.** 안 쓰면 2단계로 안 가면 됨
- 기존 `notices`(학생 공지) 구조가 참고 사례

### 2단계 — 1:1 대화
- 실시간 채팅, 안 읽음 표시, 접속 상태 연동(이미 있는 `presence` 재사용)
- 데스크톱 알림
- 검색은 현재 대화 내 클라이언트 검색까지만

### 3단계 — 그룹·부서 대화
- `teacherAssignments.department` 기반 부서 채팅방 자동 생성
- 전체 공지 채널(관리자만 발신)

### 4단계 — 파일 전송·보관 정책
- Storage 연동, 용량/확장자 제한
- 학기/학년도 단위 보관 정책과 자동 정리

### 5단계 — 모바일
- PWA + FCM 웹 푸시로 먼저 시도, 부족하면 네이티브 검토

## 5. 착수 전 결정해야 할 것

- 관리자가 다른 사람의 대화를 열람할 수 있어야 하는가? (감사 vs 프라이버시)
- 메시지 보관 기간은? (학기 단위 삭제 / 무기한 / 사용자 선택)
- 퇴직·전출 교사의 과거 대화는 어떻게 처리하는가?
- 학생·학부모도 대상인가, 교직원 전용인가? (범위가 크게 달라짐)
- 쿨메신저와 병행 기간을 둘 것인가, 특정 시점에 전환할 것인가?

## 6. 권장

**1단계(쪽지)까지만 먼저 만들고 멈춰서 반응을 볼 것.**
쪽지는 위험이 낮고, 조직도·권한·알림 기반을 실제로 검증해준다.
그 단계에서 "이거 편한데?" 소리가 나오면 2단계로, 아니면 거기서 멈춰도
잃는 게 거의 없다 — 기반 작업은 전부 다른 기능에도 쓰인다.
