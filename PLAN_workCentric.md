# 업무 중심 구조 전환 — Slack 설계 메모 검토

> 작성일: 2026-08-21
> 상태: 검토 완료 · 구현 미착수
> 입력 문서: `school_messenger_slack_design_context.md` (Slack 정보 구조 학습 메모)
> 관련: [PLAN_messenger.md](./PLAN_messenger.md) · [PLAN_dashboardElectron.md](./PLAN_dashboardElectron.md)

---

## 결론 먼저

**구조를 뒤집을 필요는 없다. 이미 조직 기억용 그릇이 있고, 학교 전체 열람이다.**

업무 글(`requests`)은 소속 교사 전원이 읽을 수 있게 이미 열려 있다. 인수인계가 깨지는
지점은 구조 전체가 아니라 **쪽지(`personalNotices`) 한 곳**에 몰려 있다 — 보낸 사람과 받은
사람만 읽을 수 있어 후임자가 접근할 수 없다.

그리고 **현재 구조는 다수 교사(수신자)에게는 이미 맞다**(§2). 부족한 것은 소수 발신자
쪽 도구다. 만들 범위가 생각보다 작다는 뜻이고, 동시에 잘못 건드리면 잘 돌아가던 다수
경험을 망칠 위험이 있다는 뜻이기도 하다.

전환의 실체는 "새 시스템을 만드는 일"이 아니라 **새는 유입 경로를 막고(§7), 이미 있는
그릇에 연도와 역할을 붙이는 일**(§6)이다.

특히 **총괄은 새 필드도 새 권한도 필요 없다** — 개입이 아니라 가시성이므로 뷰 하나면
된다(§4.2). 실제로 새로 넣을 필드는 `year`와 담당(`ownerUids`) 둘뿐이다.

---

## 1. 왜 업무 중심인가 — 공립학교라는 조건

기획·개발 과정에서는 사람 중심이 자연스럽게 느껴진다. 쿨메신저도, 이 시스템의 쪽지도
그렇게 만들어졌다. "누구에게 보낼까"가 먼저 떠오르기 때문이다.

**공립학교에서는 이 전제가 성립하지 않는다.** 3~5년 주기 전보로 담당자가 통째로 갈린다.
사기업이라면 사람 중심으로도 그럭저럭 굴러가지만, 담당자 교체가 상수인 조직에서는
사람에 묶인 기록이 주기적으로 통째로 증발한다.

업무 중심은 더 나은 설계 취향이 아니라 **이 조직 형태가 강제하는 제약**이다.

```text
사람 중심   기록이 개인의 받은편지함에 남는다  →  전보와 함께 사라진다
업무 중심   기록이 업무에 남는다              →  사람이 갈려도 남는다
```

---

## 2. 발신자와 수신자의 비대칭 — 이 문서의 판단 축

업무분장표에는 50여 명 전원에게 담당 업무가 적혀 있다. 그러나 **전체 교원을 대상으로
무언가를 진행하고 안내하는 교사는 일부다.** 특히 담임 교사는 업무를 굴리는 쪽이 아니라
부서의 안내에 따라 참여하는 쪽이다.

| | 인구 | 필요한 것 | 현재 코드 |
|---|---|---|---|
| **발신자** — 부장 · 부서 담당 | 소수 (~20) | 총괄·담당·분담, 진행 현황 | 없음 |
| **수신자** — 담임 등 | 다수 (~50) | 내 할 일, 완료 체크 | **있음** (`targetUids`·`completedUids`, 홈) |

이 비대칭이 나머지 판단의 근거가 된다.

### 2.1 채널 과다 문제가 축소된다

입력 문서 §6.1은 채널이 수십~수백 개가 되면 "어디에 써야 할지 헤맨다"고 경고한다.
그런데 **대부분의 교사는 채널을 탐색하지 않는다.** 담임은 채널을 돌아다니며 뭐가 있나
보는 것이 아니라, 부서가 안내한 것에 응답한다. 그들에게 채널이 20개든 100개든 체감
차이가 없다 — 어차피 들어가지 않는다.

즉 채널 과다의 부담은 **소수 발신자에게만** 발생한다. 전 교직원의 문제가 아니라
부장·담당자 20명의 문제로 줄어들고, 그건 훨씬 다루기 쉬운 크기다.

입력 문서 §16.5(*"사용자는 채널보다 내가 할 일을 먼저 본다"*)가 같은 관찰이다.

### 2.2 다수용 구조는 이미 맞다

수신자에게 필요한 것은 "내가 할 일"과 "완료 체크"뿐이고, 그건 이미 `Home.jsx`와
`completedUids`로 돌아가고 있다. **건드릴 이유가 적다.**

---

## 3. 채널 — 정의 기준 · 세 축 · 수명

### 3.1 정의 기준은 사람이 아니라 주제

Slack 채널은 "누가 속하나"가 아니라 **"무엇에 대한 공간인가"**로 정의된다. 멤버십은
정의가 아니라 결과다. `#교무기획`은 *교무기획부 사람들의 방*이 아니라 *교무기획 업무에
대한 공간*이고, 부원들이 거기 있는 것은 결과일 뿐이다.

**다만 이것을 "채널 = 업무"로 읽으면 안 된다.** 정의 기준과 입자 크기는 다른 문제다.

### 3.2 Slack이 푸는 것과 안 푸는 것

Slack 채널 멤버십은 **권한도 책임도 아니고 구독(subscription)이다.** 공개 채널은 누구나
읽고, 참여는 "내가 이걸 따라가겠다"는 표시일 뿐이다.

그리고 **Slack에는 담당자 개념이 아예 없다.** 그래서 실무 팀들이 Jira·Asana를 따로
붙인다. Slack이 푸는 것은 *접근과 발견*이지 *책임*이 아니다.

학교 업무의 복잡함 — 1인 책임, 부장 총괄, 2~3인 분담 — 은 정확히 **Slack이 안 푸는
영역**이다. 그걸 채널로 표현하려 하면 조합마다 채널이 필요해져 채널 폭발로 간다.

### 3.3 그래서 세 축을 분리한다

| 축 | 무엇 | 어디에 붙나 |
|---|---|---|
| **접근·열람** | 누가 볼 수 있나 | 채널 — 기본 공개 |
| **구독·알림** | 누가 따라가나 | 채널 참여 (업무분장 기본값 + 본인 조정) |
| **책임** | 누가 굴리나 | **업무(Work Item)** — 채널 아님 |

이렇게 나누면 1인 책임이든 3인 분담이든 **채널은 하나도 늘지 않는다.** 전부 업무 단위
역할로 표현된다.

> ⚠️ **3.4~3.5는 재검토 중이다(2026-08-21 저녁).** 아래는 "채널 = 상설 영역"을 전제로 쓴
> 내용인데, 이후 논의에서 **채널 = 업무**로 기울었다. §12 참고 — 업무분장표 기반 실제 업무
> 목록을 놓고 판단하기로 하고 보류한다.

### 3.4 진짜 위험은 헷갈림이 아니라 연도 반복

"채널이 많아서 헷갈린다"는 §2.1에서 보듯 약한 논거다. 강한 논거는 따로 있다 —
**학교 업무는 매년 반복된다.**

```text
업무별 채널
  #2026-1학기-2차고사   #2026-9월-학력평가   #2026-교직원연수
  #2027-1학기-2차고사   #2027-9월-학력평가   #2027-교직원연수
      ↑ 시간축으로 무한 증식

상설 영역 채널
  #교무기획 (고정)
    └ 2026 1학기 2차고사 · 2027 1학기 2차고사 …
      ↑ 채널은 그대로, 안에 연도별로 쌓인다
```

하나를 계속 재사용하면 연도 구분이 사라지고, 매년 새로 만들면 증식한다.
이것은 §6.1의 `year` 스코프 결손과 같은 문제의 다른 얼굴이다.

### 3.5 상설 + 한시 병존 (권장)

둘 중 하나를 고를 필요가 없다. Slack 실무도 정확히 이 방식이다 — 상설 채널과 `#proj-*`
한시 채널을 함께 쓰고, 끝나면 보관한다.

| 종류 | 예 | 수명 | 판단 기준 |
|---|---|---|---|
| **상설 채널** | `#교무기획` `#2학년` `#과학` | 영구 | 매년 반복되는 업무가 여기 쌓인다 |
| **한시 채널** | `#학교축제-2026` `#교육과정개편` | 끝나면 **보관** | 한 번짜리 큰 프로젝트 |
| **채널 없이 글로** | 작은 업무·단발 안내 | — | 상설 채널 안의 글 하나 |

판단 기준 한 줄: **"이게 매년 반복되나?"**

---

## 4. 책임 — 총괄은 뷰, 담당은 필드

### 4.1 학교의 책임 역할은 넷이다

| 역할 | 성격 | 현재 코드 |
|---|---|---|
| **총괄** | **진행 상황을 볼 수 있다. 그 이상도 이하도 아니다** | 권한은 이미 있음 (§5.2) |
| **담당** | 굴리는 사람. 1명 또는 N명 | 없음 |
| **분담** | 각자 자기 몫 조각 | 없음 |
| **대상** | 완료 체크해야 하는 전체 | `targetUids` + `completedUids` — **있음** |

### 4.2 총괄의 정의 — 개입이 아니라 가시성

**총괄은 "업무가 어떻게 진행되고 있는지 확인할 수 있다"는 뜻이다. 그 이상도 이하도 아니다.**
일은 기본적으로 담당자가 한다. 부장은 그걸 대신 하거나 승인하는 사람이 아니라, **알아야
하는 사람**이다.

이 정의가 설계를 크게 단순화한다.

- 총괄은 **워크플로 권한이 아니다** — 승인·반려·개입 단계를 만들 필요가 없다
- 총괄은 **읽기**다 — 그리고 업무 글은 이미 소속 교사 전원 읽기 가능이다(§5.2)
- 따라서 **총괄자 필드를 새로 만들 필요가 없다.** 필요한 것은 필드가 아니라 **뷰**다

```text
총괄 = 필터( 어느 조직의 업무인가 ) × ( 내가 그 조직의 부장인가 )
       ↑ 업무에 조직 연결 필요        ↑ deriveRank(positionLabel) — 이미 있음
```

`apps/shared/lib/targeting.js`가 이미 `RANK_RULES`로 관리자·부장·기획·일반을 업무분장표에서
판정한다. 사람 이름을 박는 것이 아니라 *자리*에 붙으므로, 전보로 부장이 바뀌어도 총괄
관계가 끊기지 않는다 — §1의 인수인계 문제와 같은 해법이다.

### 4.3 다만 뷰가 보여줄 "진행 상황"이 아직 없다

총괄이 읽기라면, 읽을 내용이 데이터에 있어야 한다. 지금 업무 글에서 뽑을 수 있는 진행
지표는 **대상자 완료율**(`completedUids` / `targetUids`) 하나뿐이다.

부장이 알고 싶은 "어떻게 진행되고 있는지"가 둘 중 어느 쪽인지에 따라 필요한 것이 갈린다.

| | 지표 | 지금 |
|---|---|---|
| **(가)** | 대상자 완료율 — "시감표 확인 42/50" | **이미 계산 가능** |
| **(나)** | 업무 자체의 단계 — 준비 중 → 진행 → 완료 | 없음 (`status` 필드 필요) |

(가)만으로도 총괄 뷰는 성립한다. **(가)로 먼저 만들어 부장 반응을 보고 (나)를 판단하는 것을
권한다** — 값싸고, 틀려도 잃는 게 없다.

### 4.4 남은 역할은 "담당" 하나다

총괄이 뷰로 풀리므로, 실제로 새 필드가 필요한 것은 **담당(누가 굴리나)** 뿐이다.
지금 글에는 `createdBy`(만든 사람)만 있는데, 만든 사람과 굴리는 사람이 늘 같지는 않다.

### 4.5 "분담"은 사실 두 종류다

여기가 제일 어렵다. 2~3인 분담이 성격이 다른 둘로 갈린다.

**(a) 같은 일을 조각내서 나눔** — 시감 배정처럼 사람마다 *다른 조각*을 맡는다.
배정표·체크리스트가 필요하다. 지금 `completedUids`는 "모두 같은 일, 했나 안 했나"만
표현한다.

**(b) 한 업무에 서로 다른 역할** — 정기고사에 시간표 담당 + 고사실 담당 + 방송 담당.
이것이 입력 문서의 Work Item → Thread 구조다.

```text
📌 1학기 2차 정기고사            ← Work Item (채널: #교무기획)
   총괄  교무기획부장             ← 업무분장에서 자동 (§4.2)
   담당  고사 담당                ← 굴리는 사람
   ├ 시감표       담당 A          ← (b) 역할 분담 = Thread
   ├ 고사실 배치   담당 B
   └ 시간표 변경   담당 C
   대상  전체 교원 → 시감 확인     ← (a) 조각 배정 = 체크
```

**(a)와 (b)를 한 메커니즘으로 억지로 합치면 안 된다.** 지금 `completedUids`가 (a)의 가장
단순한 형태인데, 여기에 (b)를 밀어넣으면 구조가 꼬인다.

---

## 5. 현재 코드와 대조

### 5.1 이미 있는 것

입력 문서는 백지에서 쓰였고 이 코드베이스를 모른다. 문서가 Phase 1~4로 새로 쌓자고
하는 것 중 **상당수가 이미 돌아가고 있다.**

| 입력 문서의 제안 | 현재 코드 | 상태 |
|---|---|---|
| Announcement (확인/미확인) | `requests.kind='notice'` + `completions` | 있음 |
| Work Item (담당·상태·마감) | `kind='request'` + `completedUids`·`dueDate`·`targetUids` | 부분 (담당 없음 — §4.4) |
| 읽음 ≠ 확인 구분 (§9) | `useSeenPosts`(읽음) + `completions`(확인) | **이미 분리됨** |
| Home = 내 업무 (§10) | `Home.jsx` — 오늘 볼 것들 | 부분 (수신자 화면만 — §8) |
| DM 유지 (§12) | `personalNotices` | 있음 |
| Thread (§3.2) | `PostComments` — 글 아래 **평면** 댓글 | 부분 |
| Channel | `channels` + `requests.channelId` | 모델 상이 (§5.2) |

특히 §9의 "읽음보다 확인이 중요하다"는 학교 특화 요구는 이미 두 메커니즘으로 나뉘어
구현돼 있다.

### 5.2 충돌 — 채널이 "구성원 모임"으로 정의돼 있다

```js
// apps/shared/lib/channels.js
memberRule = { conditions, includeUids, excludeUids }   // targeting.js 조건
memberUids: members.map(m => m.uid)
```

주석에도 명시돼 있다 — *"참여자는 targeting.js의 대상 조건을 그대로 쓴다."*
즉 **현재 구현은 채널을 사람 조건으로 정의한다.** §3.1·§3.3의 판단과 반대 방향이다.

차이가 드러나는 곳은 셋이다.

1. **횡단 업무** — 학력평가는 교무기획부와 1·2·3학년부가 함께 걸린다.
   지금 글은 `channelId`를 하나만 가진다
2. **발견** — 조건에 안 맞으면 "내 채널" 목록에 안 뜬다.
   **읽을 수는 있지만 존재를 모른다** — 권한이 아니라 발견의 문제
3. **인사이동** — 조건을 재계산하면 전임자가 목록에서 빠진다.
   접근 권한은 남지만 "내가 맡은 업무 공간"이라는 연속성은 끊긴다

### 5.3 권한은 이미 열려 있다

```text
match /requests/{requestId}   allow read: if isTeacher(schoolId)
match /channels/{channelId}   allow read: if isTeacher(schoolId)
```

업무 글과 채널은 **소속 교사 전원 읽기 가능**이며 멤버십에 묶여 있지 않다.
따라서 "인사이동하면 후임자가 작년 기록을 못 본다"는 접근 권한 차원에서는 **사실이
아니다.** 후임자는 읽을 수 있다. 못 하는 것은 *찾는 것*이고, 그건 §6의 결손 때문이다.

---

## 6. 인수인계를 막는 진짜 결손 둘

### 6.1 업무 글에 학년도 스코프가 없다

`teacherAssignments`·`teacherSubjects`·`officeLayouts`는 전부 `{year}_{uid}` 형태로 연도가
박혀 있는데, 정작 업무 글에는 `year`가 없다.

그래서 **"작년 정기고사 때 뭘 했지"를 뽑을 방법이 없다.** 연도별 축적도, 연도 복제도
지금 구조로는 불가능하다. 후임자가 읽을 권한은 있는데 찾을 길이 없는 상태다.
§3.4의 채널 증식 문제도 여기서 나온다.

### 6.2 "담당자"라는 지속 개념이 없다

지금 글에 붙는 사람은 `createdBy`(만든 사람)와 `targetUids`(받는 사람)뿐이다.
**둘 다 그때 그 사람이지 역할이 아니다.**

인수인계는 본질적으로 *역할 → 사람* 매핑이 갈리는 일인데, 그 역할이 데이터에 없다.
"정기고사 담당"이라는 자리가 없고 "김OO 선생님이 만든 글"만 있다.

그런데 학교에는 그 매핑이 이미 있다 — **업무분장표**, 즉 `teacherAssignments`가 연도
스코프로 들고 있다(§4.2). 업무분장 데이터는 조직도가 아니라 **업무 라우팅 데이터**다.

---

## 7. 기억의 두 갈래 — 쪽지

| | 읽기 권한 | 성격 |
|---|---|---|
| `requests` (업무 글) | 소속 교사 **전원** | 조직의 기억 — 후임자가 읽을 수 있다 |
| `personalNotices` (쪽지) | 보낸 사람 + 받은 사람 **둘만** | 개인의 기억 — 후임자 접근 불가 |

**업무 글로 올라간 것은 남고, 쪽지로 간 것은 사라진다.** 그러므로 문제는 "구조를 어떻게
바꿀까"가 아니라 **"무엇이 쪽지로 새는가"**다.

**쪽지를 없애면 안 된다.** 교직원들이 오래 익숙해진 방식이고, 이 시스템의 쪽지도 같은
이유로 남겨두었다. 습관을 없애는 것이 아니라 더 좋은 구조로 옮기는 문제다.

이 결론은 `PLAN_messenger.md` §5의 미결 질문 *"퇴직·전출 교사의 과거 대화는 어떻게
처리하는가"*에 대한 답이기도 하다 — **업무성 대화는 애초에 개인 대화로 시작하지 않게
하는 것**이 보관 정책보다 앞선다.

---

## 8. 업무 대시보드 — 역할별로 갈려야 한다

현재 홈은 `요청받은 일` · `전체 공지` · `학사일정` 셋이다. 전부 **수신자 화면**이다.
**부장도 부원과 똑같은 화면을 본다** — "우리 부서 업무가 어떻게 굴러가는지"를 보여주는
자리가 어디에도 없다.

§4.1의 역할이 생기면 화면이 갈린다.

```text
내가 담당    내가 굴려야 하는 것         →  진행 상태 · 마감
내가 총괄    남이 하는데 내가 챙길 것    →  진행률 · 지연 경고    ← 지금 전혀 없음
내가 대상    내가 완료 체크할 것         →  지금의 "요청받은 일"
참고         안내 · 일정                →  지금의 "전체 공지"
```

부장에게 필요한 것은 "내 할 일"이 아니라 **"안 되고 있는 것"**이다.
§2의 비대칭과 겹쳐 보면 — 다수는 지금 화면 그대로 두고, 소수에게 총괄·담당 화면을
더하는 것이 맞다. 기존 화면을 갈아엎는 일이 아니다.

**총괄 뷰에는 새 권한이 필요 없다.** 업무 글은 이미 소속 교사 전원 읽기 가능이고(§5.2),
총괄은 개입이 아니라 가시성이다(§4.2). 즉 이 화면은 **이미 읽을 수 있는 것을 부장에게
쓸모 있는 모양으로 모아주는 일**이다 — 규칙도 마이그레이션도 건드리지 않는다.

---

## 9. 권고 순서

**멤버십 모델 결정보다 §6의 결손 둘이 먼저다.** 채널을 어떻게 정의하든 업무 글에 연도와
역할이 안 붙어 있으면 인수인계는 여전히 안 된다. 반대로 이 둘이 붙으면 채널 모델은 그
위에서 어느 쪽으로도 갈 수 있다.

1. **업무 글에 `year` 추가 + 백필** — `createdAt`에서 유도. 기존 데이터 안 깨짐, 즉시 가능
2. **담당(`ownerUids`) 필드 추가** — 총괄은 뷰로 풀리므로(§4.2) 필드는 담당 하나면 된다
3. **업무 ↔ 조직 연결 확정** — 총괄 뷰의 필터 기준. `channelId`를 쓸지 별도 필드를 둘지
4. **총괄 뷰 (부장 대시보드)** — 완료율(가)만으로 먼저. 기존 수신자 화면은 그대로 둔다
5. **쪽지 → 업무 글 전환 유도** — **장기전**(§9.1). 위와 병행하되 완료를 기대하지 않는다
6. **채널 멤버십 모델 결정** — 위가 끝난 뒤
7. **Work Item ↔ Thread 계층** — 가장 큰 변경. 마지막

1~4번이 한 덩어리다. 넷을 합쳐야 "부장이 자기 부서 업무 진행 상황을 본다"가 성립한다.
그리고 그것이 **소수 발신자에게 처음 생기는 도구**다(§2).

**한시 채널(§3.5)은 이 목록에 넣지 않았다** — §11 참고.

### 9.1 쪽지 전환은 기능이 아니라 습관 문제다

강제는 무리다. 한두 번 안내로 바뀌지 않고, 시간이 걸린다.
그러므로 이것은 **완료 조건이 있는 작업이 아니라 계속 굴러가는 장치**로 봐야 한다.

- **제안만 한다** — 막지 않는다. 쪽지 보내기는 언제나 그대로 동작한다
- 조건이 맞을 때(대상 다수, 첨부 있음, 마감 표현 감지) **한 줄 제안**을 띄운다
- 성과 지표는 "전환율"이 아니라 **업무 글 비중이 학기 단위로 느는가**이다
- 실패해도 잃는 것이 없다 — 기존 쪽지 경험은 건드리지 않았으므로

---

## 10. 남은 격차

| 항목 | 지금 | 비고 |
|---|---|---|
| Work Item 아래 여러 Thread | 글 하나에 평면 댓글 하나 | 목표: 정기고사 → 시감표·고사실·방송 |
| 일반 대화 스트림 | 채널에 "글"만 있음 | **의도적 결정** — 쿨메신저 병존 중 대화 분산 우려. 뒤집으려면 그 판단부터 |
| 횡단 업무 | `channelId` 단일 | 목표: `related_organizations[]` |
| 조각 배정 (§4.3 a) | `completedUids` — 전원 동일 과업 | 사람별 다른 조각을 못 표현 |
| Activity Log | 없음 | 담당자·마감·상태 변경 이력 |
| 연도 복제 · 인수인계 화면 | 없음 | §6.1이 선행 조건 |
| 채널 보관(archive) | 없음 | 한시 채널과 함께 **보류**(§11) |

---

## 11. 결정된 것 / 남은 것

### 결정 (2026-08-21)

| 항목 | 결정 |
|---|---|
| **총괄의 정의** | **진행 상황을 확인할 수 있다는 것. 그 이상도 이하도 아니다.** 일은 담당자가 하고, 부장은 아는 사람이다. 승인·개입 없음 → 필드가 아니라 뷰(§4.2) |
| **쪽지 → 업무 전환** | **강제하지 않는다.** 제안만. 한두 번으로 안 바뀌는 습관 문제이므로 장기전으로 다룬다(§9.1) |
| **AI 기능** | **한참 뒤.** 지금 단계에서 볼 것이 아니다. 구조가 자리 잡은 뒤 재검토 |

### 남은 것

- **업무를 어느 조직에 붙일 것인가** — 총괄 뷰의 필터 기준(§9의 3번).
  `channelId`를 그대로 쓸지, 별도 조직 필드를 둘지. 횡단 업무(§5.2)를 어떻게 볼지와 엮인다
- **부장이 보고 싶은 진행 상황이 (가)인가 (나)인가** — 완료율이면 지금 데이터로 되고,
  단계면 `status` 필드가 필요하다(§4.3). **(가)로 먼저 만들어보고 판단 권장**
- 채널에 "일반 대화"를 허용할 것인가 — 쿨메신저 병존 판단을 뒤집는 문제

### 보류

- **한시 채널(§3.5)** — 누가 만들 수 있게 할지 판단이 서지 않는다.
  **일단 만들지 않는다.** 상설 채널만으로 시작하고, 실제로 "이건 채널이 필요한데" 소리가
  나오면 그때 만든다. 안 만들어서 겪는 불편은 눈에 보이지만, 만들어놓고 방치된 채널이
  쌓이는 것은 되돌리기 어렵다
- **(a) 조각 배정 vs (b) 역할 분담**(§4.5) — 소수 발신자 쪽 설계라 우선순위가 뒤로 간다.
  §9의 4번 대시보드를 만들어보고 판단해도 늦지 않다

---

## 12. 검토 중 — 채널 = 업무 방향 (데이터 대기)

2026-08-21 저녁 논의에서 §3의 전제("채널 = 상설 영역")가 흔들렸다. 결론을 내리지 않고
**업무분장표 기반 실제 업무 목록을 놓고 판단하기로 한다.** 주말에 작성 예정.

### 12.1 채널 = 업무로 기운 이유

1. **하단 입력창이 성립한다** — 채널이 업무면 대상이 곧 그 업무 관계자다.
   `PostNew`의 필수 조건(`title` + `targets.length > 0`) 중 대상이 자동으로 채워지고,
   제목은 첫 줄에서 뽑을 수 있다. Slack처럼 채널 하단에 입력창을 상시로 둘 수 있다
2. **연도 증식이 결함이 아니라 인수인계 단위가 된다** — §3.4에서 증식을 위험이라 적었으나
   조건부였다. 보관(archive)이 있으면 `#2025-1학기-2차고사`를 통째로 열어보는 편이
   상설 채널에서 작년 글을 찾는 것보다 **인수인계에 낫다**
3. **§6.1의 `year` 결손이 채널 레벨에서 풀린다** — 글마다 연도를 붙일 필요가 없다
4. **Work Item 계층을 따로 안 만들어도 된다** — 채널이 곧 Work Item이다.
   §9의 7번(가장 큰 변경)이 상당 부분 사라진다
5. **일반 대화를 안 하기로 한 결정(§11)이 부서 채널의 존재 이유를 약하게 만든다** —
   부서 채널에 올라올 것이 결국 업무 글뿐이라면, 부서는 채널이 아니라 채널에 붙는
   `department` 필드로 충분하다. 총괄 뷰(§4.2)가 그 필터로 동작한다

### 12.2 가시성(visibility) 축이 새로 필요하다

업무 채널이 기본적으로 전 교직원 대상이더라도, **담당자 그룹을 지정하면 그들에게만
보여야 하는 업무**가 있다 — 부장회의, 학업성적관리위원회, 교육과정위원회 등.

```text
채널.visibility
  'open'   (기본)  아무 지정 없으면 전체 교직원 열람.
                   memberUids는 "대상"이지 "권한"이 아니다
  'closed'         memberRule 조건에 맞는 사람만 열람
```

**이 축이 §5.2에서 "충돌"이라 적은 조건 기반 멤버십을 구제한다.** 조건 기반은 공개
채널에는 과하지만 **비공개 채널에는 정확히 옳은 도구**다 — `rank == '부장'`으로 두면
인사이동으로 부장이 갈려도 자동 승계된다. 총괄을 `deriveRank`로 유도한 것과 같은 원리다.

> **함정**: 비공개 채널의 접근권을 *명단*으로 박으면 우리가 풀려던 문제가 되돌아온다.
> 읽기를 `targetUids`(발송 시점 고정)로 제한하면 **내년 위원이 작년 회의 기록을 못 본다.**
> 위원회야말로 매년 구성원이 갈리는 자리다. 반드시 **조건**으로 판정해야 한다.

구현은 둘 중 하나다.

| | 방식 | 대가 |
|---|---|---|
| (1) | 규칙에서 상위 채널을 `get()` | 항상 최신. 글마다 읽기가 한 번씩 더 붙는다 — 목록 쿼리 비용 확인 필요 |
| (2) | 글에 `readableBy` 비정규화 + 멤버 변경 시 일괄 갱신 | 규칙 한 줄, 추가 읽기 없음. Cloud Function 배치 필요 (인사이동은 연 1회) |

**(2) 권장** — `memberUids`·`completedUids`를 이미 같은 이유로 비정규화하고 있어 패턴이 일관된다.

총괄 뷰도 가시성을 타야 한다. 부장회의를 부장만 본다면 그 채널의 총괄은 교감·교장이다.
총괄도 조건이고 가시성도 조건이므로 둘을 AND로 걸면 된다.

### 12.3 데이터로 답할 질문

주말 업무 목록이 나오면 다음을 **세어서** 판단한다. 책상에서 답이 안 나오는 것들이다.

- **채널감 / 글감 기준** — 며칠 이상, 몇 단계, 몇 명이면 채널인가.
  전자칠판 고장 하나로 채널을 만들 수는 없다
- **`closed` 비율** — 비공개가 필요한 업무가 전체의 몇 %인가.
  드물면 예외 처리로 충분하고, 흔하면 일급 개념이어야 한다
- **`closed` 생성 권한** — 아무나 만들면 "보이지 않는 방"이 늘어난다.
  좁은 조직에서는 그 자체가 정치적 부담이 될 수 있다. 부장 이상으로 제한할지
- **부서 채널 약화가 실제로 맞는가** — 부서 단위 소통이 업무 글 말고 실재하는지
- **총괄 자동 유도가 맞는가**(§11 남은 것) — 부장이 실제로 총괄하는 업무 비율
- **(a) 조각 배정 vs (b) 역할 분담 비중**(§4.5)
- **반복성** — 매년 반복 / 학기마다 / 단발의 비율. 연도 인스턴스 설계의 근거

### 12.4 기록해 둘 항목 (업무별)

분석이 가능하려면 업무마다 다음이 있어야 한다. 서술형이면 세는 것이 불가능하다.

```text
업무명 · 담당 부서 · 담당자 수 · 총괄이 누구인가
대상 범위 (전 교원 / 일부 조건 / 담당자끼리만)
가시성 (공개 / 비공개 — 비공개면 이유)
기간 (하루 / 며칠 / 몇 주 / 상시)
반복 (매년 / 학기 / 수시 / 단발)
분담 방식 (조각 a / 역할 b / 해당없음)
```
