# 블록 편집기 — 추후 작업

> 상태: 계획만. 착수 전 아래 "판단이 필요한 것"부터 정할 것.

## 하고 싶은 것

1. **요소별 위치 이동** — 문단·제목·목록을 잡아서 위아래로 옮기기 (노션의 ⠿ 손잡이)
2. **엔터를 칠 때마다 기본 서식으로** — 제목 줄에서 엔터를 치면 다음 줄은 본문이어야 함
3. **요소별 서식 변경** — 블록 하나를 골라 제목 ↔ 인용 ↔ 목록으로 바꾸기

## 왜 지금 구조로는 어려운가

지금 편집기(`RichTextEditor.jsx`)는 `contentEditable` + `document.execCommand`다.
번들 크기를 아끼려고 ProseMirror·TipTap 같은 편집기 프레임워크를 일부러 피한 선택이었고,
글을 쓰고 서식을 넣는 수준까지는 그 선택이 맞았다.

문제는 위 세 가지가 전부 **"블록이라는 단위가 코드에 존재해야" 되는 일**이라는 점이다.
지금은 블록이라는 개념이 없다. `editor.innerHTML` 문자열 하나가 전부고, `<h2>`인지
`<p>`인지는 브라우저가 만들어놓은 DOM을 그때그때 훑어서 짐작한다.

- **위치 이동**에는 "이 블록"을 가리킬 방법이 필요하다. 지금은 문단에 ID도 없고,
  DOM을 옮기면 `innerHTML`이 통째로 바뀌면서 커서가 어디 있었는지 잃는다.
- **엔터 처리**는 브라우저 기본 동작에 맡겨져 있다. 제목 줄에서 엔터를 치면 브라우저가
  `<h2>`를 이어서 만든다. 이걸 막으려면 엔터를 가로채 직접 블록을 만들어야 하는데,
  그러면 이미 블록 모델을 손으로 짜고 있는 셈이다.
- **요소별 서식 변경**도 "지금 고른 블록"이 있어야 성립한다.

여기에 `execCommand`가 이미 신뢰할 수 없다는 증거가 쌓였다.

- `formatBlock`은 **빈 줄에서 아무 일도 하지 않는다.** `/제목`을 첫 줄에서 고르면
  글자만 지워지고 제목이 안 생기던 버그(`43168bf`)가 이것 때문이었다. 빈 줄일 때만
  블록을 직접 만들어 끼우는 것으로 우회했다.
- 한글 입력 중에는 Enter가 '글자 확정'이라 메뉴 선택과 부딪힌다. `/구분선`을 쳤을 때
  '선'이 남던 문제가 이것이었고, `isComposing` 가드로 막았다.
- `execCommand` 자체가 표준에서 폐기된 API다. 브라우저마다 결과 DOM이 다르다.

우회가 하나씩 쌓이고 있고, 세 기능을 그 위에 올리면 결국 **블록 편집기를 직접 만드는
일**이 된다. 그건 이 프로젝트가 감당할 범위가 아니다.

## 판단이 필요한 것

### 갈래 A — TipTap(ProseMirror)으로 교체

블록 모델·엔터 처리·드래그 이동·per-block 메뉴가 전부 기본으로 딸려 온다.
세 요구사항이 "구현"이 아니라 "설정"에 가까워진다.

치를 것:
- **번들이 커진다.** 얼마나 커지는지는 재보고 정할 것 — 대시보드는 노트북에서 쓰고
  학교 망이 빠르지 않다. 초기 판단이 "크니까 쓰지 말자"였으므로, 이번에는 숫자를 보고
  다시 판단해야 한다.
- 저장 형식이 바뀐다. 지금 `bodyHtml`에 든 HTML을 그대로 읽을 수는 있지만, TipTap의
  JSON 문서 모델로 옮기는 편이 이득이 크다. 그러면 기존 글 마이그레이션이 필요하다.
- `sanitizeHtml`·`htmlToText`가 손이 간다.

### 갈래 B — 지금 구조를 유지하고 2번만 고친다

**엔터 서식 복귀는 나머지 둘과 달리 지금도 할 수 있다.** 엔터를 가로채서 "현재 블록이
제목·인용이면 다음 줄은 `<p>`로 시작"만 처리하면 된다. 블록 모델 없이 된다.

1번(위치 이동)과 3번(요소별 서식)은 포기하거나 미룬다.

### 갈래 C — 쓰는 곳을 나눈다

업무 글·쪽지는 지금 편집기로 충분할 수 있다. 학교에서 오가는 글은 대개 짧고, 문단을
드래그해 옮길 만큼 긴 문서는 드물다. 정말 긴 문서(교육과정 계획, 매뉴얼)를 다룰 자리가
생길 때 그 화면에만 무거운 편집기를 붙인다.

## 먼저 확인할 것

- [ ] 지금 엔터 동작이 실제로 어떤지 — 제목 줄에서 엔터를 치면 다음 줄이 제목인가?
      (그렇다면 2번은 기능 추가가 아니라 버그 수정에 가깝다)
- [ ] TipTap 최소 구성의 실제 번들 증가량 측정
- [ ] 지금까지 쓴 글에서 실제로 몇 줄짜리 글이 오가는지 — 드래그 이동이 정말 필요한가

## 참고

- 편집기: `apps/dashboard/src/components/RichTextEditor.jsx`
- 슬래시 메뉴: `apps/dashboard/src/components/SlashMenu.jsx`
- 걸러내기: `apps/shared/lib/richText.js`
- 쓰는 곳: 업무 글쓰기(`PostNew.jsx`), 쪽지(`NoticeComposeModal.jsx`)
