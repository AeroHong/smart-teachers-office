2026.4.8.(수) — 버그 발견
1) ✅ login 이후 메인 페이지로 넘어가지 않고 멈추는 문제 발생
2) ✅ 대시보드 출결현황 페이지에서 날짜 선택이 무조건 2026.4.7. 로 고정됨; today 로 자동 설정되어야 함
3) ✅ 이벤트 생성 페이지에서 대상 학생 그룹을 설정하는데, 대상 그룹을 별도로 작성하게 되어있음; 불필요한 입력이므로 삭제
4) ✅ 학생명단 페이지에서 이미 생성한 학생 그룹 목록이 나오지 않음; 담당 교사가 만든 그룹은 수동 편집도 가능해야함
5) ✅ (우선처리) 학생이 QR코드를 이용에 페이지로 들어갔는데 오류 발생 - 잠시 후 다시 시도해 주세요. 라고 나옴 ; 학생들이 스마트폰으로 스캔할 때 로그인 페이지가 안나옴 - 로그인 여부를 확인하고 비로그인 상태라면 바로 로그인 페이지 (구글로 로그인) 가 나오도록 수정해야함 ; 로그인 했는데 학생 목록에 없으면 '시간표를 확인하세요. 출결 목록에 없습니다.' 등의 경고 메시지가 나오도록 하면 됨

→ 1)~5) 2026.4.10. 해결 완료
→ 6) 추후 개발 기능으로 분리 (추후개발.md 참고)

---

2026.4.10.(금) — 현장 사용 중 발견

1) 🔴 주 반복 이벤트에서 날짜 계산 오류
   - 금요일로 설정한 반복 이벤트인데 출석 기록이 4월 9일(목)로 저장됨
   - 실제 로그인 시간은 4월 10일(금)
   - 원인 추정: 요일 기반 날짜 계산 로직이 "가장 가까운 과거 해당 요일"이 아닌 다른 기준으로 동작
   - 수정 방향: 출석 체크 시점의 실제 날짜를 기준으로 저장해야 함

2) 🔴 동일 이벤트 중복 출석 가능
   - 같은 날 같은 이벤트에 20분 간격으로 2회 출석 등록됨
   - 첫 번째 출결: 4월 9일 날짜, 두 번째 출결: 4월 10일 날짜 (날짜 오류와 연관)
   - 수정 방향: 동일 학생 + 동일 이벤트 + 동일 날짜 조합으로 중복 방지 로직 강화

3) 🟠 반복 이벤트 내 요일-교시 구분 DB 저장 구조 확인 필요
   - 1개 이벤트 안에서 요일-교시로 구분되는 경우, 출석 로그가 요일별로 분리 저장되는지 확인 필요
   - 현재 출석 DB 구조:
     checkedAt (timestamp), class (int64), grade (int64), late (boolean),
     method (string), number (int64), qrToken (string), studentId (string), studentName (string)
   - 미결정: 이벤트 설계 방향 → 아래 설계 결정 사항 참고

4) 🟠 결석 사유 저장 후 화면 미반영
   - DB에는 사유가 정상 저장되었으나 화면에 표시 안 됨
   - 사유 등록 카운트도 반영 안 됨
   - 원인 추정: 실시간 구독(onSnapshot) 범위에서 사유 필드 누락 또는 상태 업데이트 미처리

---

## 🤔 설계 미결정 사항

**이벤트 생성 구조** — 결정 필요:

| 방식 | 설명 | 장점 | 단점 |
|------|------|------|------|
| A안 | 과목명-요일-교시 로 이벤트 개별 생성 | 구조 단순, QR 분리 명확 | 이벤트 수 많아짐 |
| B안 | 과목명으로 생성 후 내부에서 요일-교시 구분 | 이벤트 수 적음 | DB/로직 복잡도 증가 |

→ 이 결정에 따라 1), 2), 3) 버그 수정 방향이 달라짐

**✅ 2026.4.10. 결정: A안 + 과목 그룹핑**
- DB/로직은 A안 (이벤트 = 요일-교시 단위로 각각 생성)
- 화면에서는 과목(Course) 단위로 그룹핑해서 표시
- 과목 컬렉션 신설: /schools/{schoolId}/courses/{courseId}
- 각 이벤트에 courseId 필드 추가
- 통계도 과목 단위로 집계
