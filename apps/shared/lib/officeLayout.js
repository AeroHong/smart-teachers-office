/**
 * 사무실 자리 배치 문서 ID 규칙.
 *
 * 실제 구현은 @shared/lib/schema.js 로 옮겼다.
 * 컬렉션 경로와 문서 ID 규칙을 한 파일에 모아 두기 위해서다.
 * 기존 import 경로(@shared/lib/officeLayout)를 쓰는 코드를 위해 그대로 재수출한다.
 *
 * 새 코드는 schema.js 에서 직접 import 할 것.
 */
export { officeLayoutId } from './schema'
